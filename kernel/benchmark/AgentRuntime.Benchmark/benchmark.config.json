{
  "_comment": "尺子自身的配置（与 Runtime 的 config.json 分开管）：被测物的端点/密钥走 runtimeConfig 指向的那个文件。相对路径一律相对本文件所在目录解析。",

  "runtimeConfig": "../../src/AgentRuntime.Cli/config.json",

  "_pricing_comment": "单价（USD / 1M tokens），取自 OpenClaw 的 deepseek provider 配置；改动价格要写明日期。",
  "pricing": {
    "input": 0.14,
    "cacheRead": 0.028,
    "output": 0.28,
    "source": "openclaw.json models.providers.deepseek.models[deepseek-v4-flash].cost"
  },

  "runsDir": "../runs",

  "_corpus_comment": "冻结上下文语料：按「铁则 → 知识 → 记忆」堆叠，**只用仓库内 corpus/ 的去隐私 10K 切片**（可公开，他人可复现同一档位）。⚠️ **禁止**改成私有全量语料或本机路径——lock 节是硬闸门，违反即拒绝执行（主人 2026-09-14 定死）。快照落 local-corpus/（私有，勿发布）。",
  "corpus": {
    "lock": {
      "_comment": "实验语料锁：测试工程只允许这 3 个文件、冻结规模 ≤10k token、指纹必须与 corpus.lock.json 一致。改语料必须同步：python3 benchmark/tools/lock-corpus.py --update",
      "allowedSources": [
        "corpus/rules.md",
        "corpus/knowledge.md",
        "corpus/memory.md"
      ],
      "maxFrozenTokens": 10000,
      "charsPerToken": 1.88,
      "fingerprintFile": "corpus.lock.json"
    },
    "sources": {
      "rules": "corpus/rules.md",
      "knowledge": "corpus/knowledge.md",
      "memory": "corpus/memory.md"
    },
    "tiers": [
      {
        "id": "L0",
        "label": "最小字节版（不冻结上下文）",
        "slices": []
      },
      {
        "id": "L1",
        "label": "1k 级（冻结：铁则）",
        "slices": [
          { "source": "rules", "chars": 1750 }
        ]
      },
      {
        "id": "L2",
        "label": "10k 级（冻结：铁则 + 知识 + 记忆）",
        "slices": [
          { "source": "rules", "chars": 4330 },
          { "source": "knowledge", "chars": 8607 },
          { "source": "memory", "chars": 5053 }
        ]
      }
    ]
  }
}
