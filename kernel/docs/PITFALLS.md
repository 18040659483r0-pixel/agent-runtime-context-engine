# AgentRuntime 踩坑集（L3 最细颗粒度，素材层）

> **本文件是公开变体**（由 `tools/build-public-pitfalls.py` 从内网坑集生成，**勿手改**）：内容只做两类处理 ——
> ① 本机/内网路径与人名换成中性占位符（`<repo>` / `<workspace>` / `<home>` / 作者）；② 仍会命中发布禁字的内容置为 `‹redacted›`。
> 工程结论**一字未删**。复算：`python3 tools/build-public-pitfalls.py --check`（绿 = 与源一致且 0 硬红线）。


> 通用方法论查 `workspace/METHODOLOGY.md`（L1/L2）；本文件只记本项目具体坑。

## 1. dotnet 不在 PATH（2026-09-14，Mac）

- **现象**：`which dotnet` 找不到，但 SDK 已装在 `~/.dotnet`（10.0.400，2026-08-18 装的）。
- **处理**：① 软链 `ln -sf ~/.dotnet/dotnet ~/.local/bin/dotnet`；② 在 `~/.zshrc` 追加 `export PATH="$HOME/.local/bin:$PATH"`（带注释，可随时删）。
- **⚠️ 关键坑**：**光做软链不够**。`~/.local/bin` 只在 OpenClaw 网关进程的环境里存在，**新开终端的登录 shell 里没有** —— 用 `env -i HOME=$HOME /bin/zsh -lic 'command -v dotnet'` 才能验证真相（普通 `zsh -lic` 会继承父进程 PATH，给出假阳性）。
- **没做**：没装 brew cask（需 sudo、且会再装一份 SDK）；`/usr/local/bin` 属 root 不可写。
- **跨端注意**：本解决方案**刻意不写 `global.json`**，避免两端 SDK 补丁号不同导致 `dotnet build` 直接失败；Windows 侧用 `dotnet --list-sdks` 确认 10.x 即可。

## 1.5 xUnit v2 → v3 迁移（2026-09-14）

- **版本选择**：`xunit.v3` **3.2.2**（最新 v3 稳定）+ `xunit.runner.visualstudio` **3.1.5**（v3 线）+ `Microsoft.NET.Test.Sdk` **18.10.0**；**不上 v4.x**（作者定）。
- **csproj 必改**：加 `<OutputType>Exe</OutputType>` —— v3 的测试工程是**可执行程序**（自带 runner），不是库。
- **分析器新规则 xUnit1051**：所有接受 `CancellationToken` 的调用都要传 `TestContext.Current.CancellationToken`，否则 build 出警告；**故意传自定义 token 的用例**（如“已取消的 token 会向上传播”）用 `#pragma warning disable xUnit1051` 包住并注明原因——不要为了消警告把测试意图改掉。

## 2. `System.Text.Json` 默认会把中文转义成 `\uXXXX`（2026-09-14）

- **现象**：测试断言请求体含 `"content":"你好"` 失败 —— 实际发出的是 `"content":"\u4F60\u597D"`。
- **两层处理**：
  - **线格式**：写请求时用 `JavaScriptEncoder.UnsafeRelaxedJsonEscaping`，中文原样发 UTF-8。理由：① 字节更少；② 上游分词更贴近自然中文（转义序列会把一个汉字拆成多个 token）→ 直接影响 Benchmark 的 Token 指标。
  - **测试**：不要用「原始 JSON 字符串包含中文」断言，改为 `JsonDocument` 解析后断言值；原始串断言只用于 ASCII（键名、字段存在性）。
- **反例（不要做）**：为了让字符串断言通过而关掉转义、却忘了这是**协议行为**——它是有意决策，必须有测试钉住（`中文原样发UTF8_不转义为unicode序列`）。

## 3. `HttpResponseMessage` 的 Content 读取与析构（2026-09-14）

- Provider 里读响应体必须用 `await content.ReadAsStringAsync(ct)`（带取消），并在 `using (httpResponse)` 内完成；
  否则取消时可能拿到半截 body 或被提前 Dispose。

## 4. 测试要「零网络」才能当尺子的地基（2026-09-14）

- Runtime 测试用 `FakeModelClient`；Provider 测试用 `StubHttpMessageHandler` 拦请求，断言 URL / 头 / body。
- 真实调用只走 CLI + mock provider（`tools/mock-provider/`）或真实端点，**不进单元测试**——否则测试受网络与费用影响，不再是确定性证据。

## 5. OpenAI 兼容端点的 baseUrl 语义（2026-09-14）

- DeepSeek 官方：`baseUrl = https://api.deepseek.com` **不带 `/v1` 也能通**（拼出 `/chat/completions`）；DeepInfra 则要带 `/v1/openai`。
- **不要自动补 `/v1`**：OpenClaw 侧已被这个坑咬过（带不带 `/v1` 语义不同、拼错就 404）。坚持「baseUrl 写什么就拼什么」，只做去尾斜杠。

## 6. 模块管线：用户消息只能由引擎追加（2026-09-14）

- **规矩**：模块的 `ContributeAsync` 只允许贡献「历史 / 规则 / 知识」类消息；**本轮用户消息由引擎统一 add 在最后**。
- **为什么**：如果让模块自己断言用户消息位置，一旦增减模块，消息顺序就会漂移 —— 消融实验立刻失效（对比的就不是「只差一个模块」了）。
- `SessionModule.ObserveAsync` 靠「最后一条即本轮用户输入」这个不变量取用户文本 —— 这个不变量由引擎保证，不要在模块里重复实现。

## 7. 消融测试怎么写才真的能守（2026-09-14）

- **不要**只断言「带 A+B 时消息数 = 3」。要断言：**去掉 A 后，剩余的贡献逐字相同**：
  ```csharp
  var expected = withBoth.Request.Messages.Where(m => m != rules).Select(m => (m.Role, m.Content)).ToArray();
  Assert.Equal(expected, withOne.Request.Messages.Select(m => (m.Role, m.Content)).ToArray());
  ```
- `ChatMessage` 是 **class 不是 record**，`m != rules` 是**引用比较** —— 这里正好是我们要的语义（“就是那一条实例”），不要改成值相等。

## 8. 上游前缀缓存按 64-token 块对齐（2026-09-14 实测）

- **实测**：同一 1810-token 前缀，第 1 次 `cache_hit=0`，第 2 次 `cache_hit=1664`（= **64 × 26**）→ 命中按 64 token 块计算；短前缀（几十 token）**永远 miss**。
- **别误判**：`cached = 0` ≠ 上游不支持缓存，很可能只是**前缀太短 / 前缀不稳定**。先用 T02 长前缀探针确认，再下结论。
- **对架构的含义**：想让缓存真省钱，必须把「稳定前缀」堆长且逐字节不动（→ V2 Frozen Context / V3 Append Stream）；动态内容一律往后放。
- 后续 55 次调用的阶梯实验再次印证：所有命中量都是 64 的整数倍（896/9856/256）。

## 9. 变体隔离：salt 必须放「冻结前缀最前面」（2026-09-14 自己踩的坑）

- **错误写法**：`$\"{tierText}\\n<<< end · {salt} >>>\"` —— salt 在尾部，前缀主体仍相同。
  后果：校准调用与前面的变体**已经把这个前缀烘热了** → 后面变体第 1 轮就直接命中（L2 实测 t1 命中 9856），**看起来像“运行时自动吃到缓存”，实际是实验污染**。
- **正确写法**：`$\"«{salt}»\\n{tierText}\"` —— salt 在最前，不同变体/档位**连一个字节的公共前缀都没有**。
- **自检手段**：每个变体**第 1 轮必须 cached = 0**（冷启动）。不为 0 就说明还有外部预热，本次数据作废。
- **为何容易忽略**：污染不会报错，只会让数字**变好看**。这类“沉默的乐观偏差”必须用纪律拦，不能靠眼看。

## 10. SVN 把中文 `.md` 判为二进制（`svn:mime-type=application/octet-stream`）（2026-09-14）

- **现象**：`svn add` 新目录下的中文 `.md` 时输出 `A  (bin)`，并被自动附上 `svn:mime-type=application/octet-stream` → SVN 不再当文本处理（**diff / 合并失效**；本工作区所有知识文件都靠 diff/合并）。
- **同源症状**：`svn diff` 报 `E000022 不能把字符串从 UTF-8 编码转换为本地编码`。**根因**：本机 `LANG` / `LC_ALL` 为空（C/POSIX locale）。
- **处理**：① `svn propset svn:mime-type "text/plain; charset=utf-8" <文件>` 覆盖；② SVN 命令统一带 `LC_ALL=en_US.UTF-8`。
- **不要做**：别给这些文件设 `svn:eol-style=native` —— 语料 / 冻结前缀要求**逐字节 LF 稳定**，Windows 检出转换会破坏字节、进而使前缀指纹变化、缓存整体失效。
- **适用范围**：跨端（001 端同样会遇到）→ 收尾时提升进 skill `svn-workflow`。

## 11. 基准里「变体共用 salt」→ 跨变体预热（2026-09-14，自伤）

- **现象**：T07 首版三个变体（`v1→v1` / `v1→追加尾部` / `v1→改头部`）**共用同一个 salt**（前缀最前那行相同）→ 变体 A 烘热的前缀被 B、C 直接命中：B、C 首轮 `cached=9472`（本应 0）。对照立刻失真，而且**症状看起来像「架构太好了」**。
- **铁律**：salt 必须**逐变体独立**——同一 run 内任何两个变体不得共享前缀字节。
- **守它（别靠肉眼）**：每变体首轮做**冷启动自检**，`cached != 0` 即打印告警并判该变体数据作废。
- **同类**：`--calibrate` 的校准调用也会烘热前缀 → 必须与变体前缀不同（salt 之后发生更保险）。
- 相关：`workspace/METHODOLOGY.md` §十·6；skill `llm-context-benchmark`。

## 12. `records.jsonl` 带 UTF-8 BOM（2026-09-14 发现，未修）

- **现象**：`benchmark/runs/*/records.jsonl` 首字节是 BOM → 严格解析器（`json.loads` 默认 utf-8）直接报 `Unexpected UTF-8 BOM`。
- **临时**：读的时候用 `encoding="utf-8-sig"`。
- **该修**：写 `records.jsonl` 时用 `new UTF8Encoding(false)`（无 BOM）—— 与「账本类文件 UTF-8 无 BOM」口径一致（见 `CloseoutWatermark.Save` 的做法）。
- 影响面：仅原始记录读取体验；结论可复算性不受影响（加 `-sig` 即可）。

## 13. `System.Text.Json` 枚举默认序列化成数字 → 账本不可读（2026-09-14，V3）

- **现象**：事件流 JSONL 写成 `{"kind":2,...}` —— 人眼读不出类型，diff 也看不出改了哪个枚举值。
- **正解**：读写 options 都加 `Converters = { new JsonStringEnumConverter() }` → `"kind":"Skill"`。
- **同类**：不设 `JavaScriptEncoder.UnsafeRelaxedJsonEscaping` 时中文被转义成 `\uXXXX`（文件不可读、diff 全花）。
- **守它**：测试断言落盘行里 `Contains("中文正文")` 且 `DoesNotContain("\\u")`。

## 14. `--modules` 覆盖时新建配置漏拷字段（2026-09-14，自己踩的）

- **现象**：`Program.BuildModules` 为「按名字覆盖模块」新建了一个 `RuntimeConfiguration` 并**逐字段手工拷贝**；V3 新增 `Stream` 段时没同步 → `--modules … --stream <path>` 时事件流路径被**静默丢弃**（模块起来了，流是空的，还不报错）。
- **铁则**：任何「按字段复制配置」的地方，**新增配置段必须同步**；能改成 `with` 式/深拷贝就别手抄字段。
- **同类风险**：`Frozen` 一旦加新子字段，同一处也必须补齐。
- **再次命中（2026-09-15，V4）**：新增 `snapshot` 段后，`Program.BuildModules` 那份手抄拷贝又差点漏掉 —— 已同步补上（`Snapshot = config.Snapshot`）。手抄字段的地方，新增配置段时**必须搜一遍**。

## 15. 「先重跑再校验」会掩盖版本闸门告警（2026-09-14，自己踩的）

- **现象**：跑迁移器时命令写成「先重跑 A/B，再 `--check`」→ 重跑覆盖了 `_manifest.json`，于是 `--check` 只报「产物与源一致」，**版本未 bump 的告警被抹掉**（后来靠 SVN diff 才发现内容真的变了）。
- **正确顺序**（写进 `tools/frozen-build/README.md`）：**先 `--check`**（此时应报「产物与源不一致」+「内容已变但版本未 bump」）→ **再**改 `versions.json` → **再**重跑 → **再** `--check`。
- **通用化**：凡是「产物 + 账本」两件套，**校验必须在重算之前**；否则账本被自己刷新，闸门永远绿。
- 同类教训：源**会被自动化改动**（本日 22:16 skill 巡检回写 → 语料内容变），所以 `--check` 要当**入场仪式**，不是一次性验收。

## 16. 恢复分叉后，快照账本里的 `streamPath` 会「过期」（2026-09-15，V4）

- **现象**：`--resume --fork <新流>` 之后，快照里记的仍是**原流**路径（分叉当轮会自动改写一份，但用户手上的配置文件 `stream.path` 不会自己变）。下一次 `--resume` 若不显式配/带 `--stream <新流>`，就会对着原流（含孤儿尾部）恢复 → 报 `ForkRequired` 或接错文件。CLI 目前只打印一行 ⚠️，**不代改配置**。
- **处理**：分叉后自己把 `stream.path`（或 `--stream`）指向新流；恢复报告里那行「快照记录的流路径与当前不同」就是提醒。
- **通用化**：账本里的「位置」一旦被外部动作改变（分叉 / 移动 / 归档），**要么当轮重写账本、要么调用方显式传参**，不要假设它会自己跟上。

## 17. 原子写失败的注入手法：把「目标路径」建成目录（2026-09-15，V4）

- **现象**：要证明「写入失败不留半成品」，但磁盘写满 / 权限不足在单测里不好稳定复现。
- **做法**：目标路径本身建成**目录** → `File.Move(tmp, target, overwrite: true)` 必然抛 `IOException`；然后断言：① 目录还在（目标一个字节没被碰）② 目录里没有 `*.tmp-*` 残留 ③ 旧快照仍能完整读回。
- **注意**：用 `Assert.ThrowsAny<IOException>`——不同平台 / 文件系统的具体异常类型不同，别写死某个子类。

## 18. `CloseoutWatermark.StreamCursor` 是 `int`，`SessionAppendStream.Cursor` 是 `long`（2026-09-15，V4）

- **现象**：V4 把 `--closeout` 的硬编码 `0` 换成真实流游标时，两侧类型对不上。
- **处理**：`Program.StreamCursorOf` 显式判界（`> int.MaxValue` 直接报错要求归档），**不静默截断**。
- **通用化**：跨层传账本数字，窄类型必须有闸门；截断等于伪造历史。

## 19. C# 重载二义性：`Append(kind,text,source)` 与 `Append(kind,text,tag)` 同签名（2026-09-15，V4.1）

- **现象**：给「只追加流」加第二个写入口（保留标签重放 / 分叉）时，想写成 `Append(kind, text, tag)` 的重载 —— 第三个参数同为 `string`，编译器报二义（`string?` 与 `string` 不构成重载区分）。
- **处理**：**起不同的名字**（`AppendPreservingTag`），并在注释里写清「谁是新身份、谁是保身份」。
- **通用化**：可选的 `string` 参数不要靠重载表达**语义不同**的入口；语义不同就换名字，重载只用于参数个数/类型真的不同。

## 20. 渲染里顺手 `Normalize` 会改顺序 —— 测试预期必须跟着「渲染规则」而不是「内部次序」（2026-09-15，V4.1）

- **现象**：选焦按**权重降序**选出 `[E004, E002]`，但 `Render` 里做了归一化（去重 + Tag 升序）⇒ 实际渲染 `[FOCUS] E002 E004`。第一版测试按权重序写预期 → 红。
- **处理**：把「**渲染顺序的唯一家 = 归一化**」写进注释（顺序敏感的东西必须有唯一声明处），测试预期按渲染规则写，并附一行算式说明权重为什么是这几个。
- **通用化**：**内部次序**（选焦序）与**输出次序**（逐字节）是两个概念；只要输出要逐字节稳定，就必须在输出处再归一化一次，且只此一处。

## 21. 组合根「顺手重排」会把闸门变成哑门（2026-09-15，V4.1，自伤）

- **现象**：为省事，`ModuleRegistry` 里把 `focus` 统一**追加到模块列表末尾**（这样焦点天然居末）。结果：配置里把 `focus` 写在 `append-stream` **前面**也静默通过 —— 第三道闸门（"排前面必须报错"）**永远不触发**，编出来的测试还得靠手工构造模块列表才碰得到。
- **处理**：改为**保序装配**（占位 → 原位替换），闸门立刻有牙（`--snapshot` 实跑验证：配置写反 → `exit=2` + 明确报错）。
- **通用化**：闸门要守的次序，**组合根不得替它兜底**；"我帮你排好"等于"我帮你不报错"。

## 22. 量 Runtime 开销必须 `-c Release`，且要看**放大数据量**后的增量（2026-09-15，V4.1）

- **现象**：Debug 下 `focus` 组比对照组每轮多 ~4ms，看着像「焦点很贵」。Release 暖进程一测：turn 2/3 两组都是 **0.2ms**；把流从 221 条放大到 2161 条，增量**仍是不变的 ~4ms** ⇒ 那是**首轮 JIT / 类型初始化的固定成本**，不是每轮成本。
- **处理**：多轮 `--chat` 一个进程量**每轮**开销；再放一个数量级的数据量，区分「固定成本」与「线性成本」。
- **通用化**：`dotnet run` 默认 Debug；任何「开销 / 性能」结论先声明构建配置，并用**数据量放大**做对照。

## 23. 权重阈值与衰减的**联合后果**要算出来，别凭直觉（2026-09-15，V4.1）

- **现象**：`minWeight = 1.0` 且 `decay = 0.5^(Δ/半衰期)`（半衰期 40 turn）时，某标签**只自报一次**只在 Δ=0 的那个 turn 入选（1.0 ≥ 1.0）；到 Δ=1 就是 0.9828 < 1.0 ⇒ 立刻退场。看上去像 bug，其实是**公式 + 默认值的联合后果**。
- **处理**：先按规格书实现、**不擅自调默认值**，把后果（"要留在焦点里，模型需每轮继续自报"）**报给主子**再定。主子 2026-09-15 决定下调：**默认 `minWeight` 1.0 → 0.95**（报一次管 3 个 turn：Δ=1 ⇒ 0.9828、Δ=2 ⇒ 0.9659 入选；Δ=3 ⇒ 0.9494 退场）。落地处 = `FocusOptions.DefaultMinWeight`（唯一声明处，配置段默认值随之继承），并用 `Focus_SingleReport_SurvivesThreeTurns` 把留存期钉死。
- **通用化**：阈值型参数与连续型函数组合时，先算「边界样本」再判断好不好用；改默认值属于**设计决策**（主子定），且必须**钉一个测试把行为区间写死**——否则下次改半衰期会静默改留存期。

## 24. 缓存文件与快照文件的**坏文件口径不同**，别抄错（2026-09-15，V4.1）

- **现象**：`focus.json` 是**加速缓存**（流才是真相源），`snapshot.json` 是**恢复点账本**。前者坏掉 ⇒ 当"没有缓存"（重算即可，正确性不受影响）；后者坏掉 ⇒ **必须报错**（宁可说不认识，也不装作认识）。
- **处理**：`FocusCache.Load` 捕获 `JsonException/IOException` 返回 `null`；`SnapshotStore.Load` 抛 `InvalidDataException`。两处注释互相点名，防后来者"统一口径"。
- **通用化**：**能否重建**决定坏文件的处置：可重建的（缓存 / 索引）降级，不可重建的（账本 / 事实）报错。

## 25. 规格自报的**规模数字**可能与逐字正文不符（2026-09-15，协议区 R1-P）

- **现象**：`REPORT-PROTOCOL-ZONE.md` §三 自报英文协议文本「7 行 / ≈560 字符 / ≈140 token」，但把该文本**逐字**落地后实测：**6 行 / 723 字符**；按规格自己在 §四/§七 Q8e 声明的口径「英文 ~4 字符/token」估 = **181 token** ⇒ **超出声明的 ≤160 token 预算**（旧版 `DESIGN-PROTOCOL-ZONE.md` §四 的「≤600 字符」同样不满足）。
- **处理**：纪律是「§三 逐字 + 不自行改设计」⇒ **正文一个字节都不删**（删正文 = 改协议 = 缓存归零，且是静默改设计）；把**执行上限**上修到能容纳逐字文本的最小档（`ProtocolText.MaxTokens = 200`），并把真实值（181）与声明值（160）**双双钉进测试**，同时在交付报告里列为偏离 #1。
- **通用化**：① 规格里的「规模 / 预算」是**可复算量**，实现时必须**实测并回填**，不能照抄估计值 —— 估计值与逐字正文不符时，**以正文为准、把预算缺口报出来**，不要偷偷改正文也不要偷偷放宽预算；② 两个量（正文 / 预算）的**唯一声明处**要放在一起（`ProtocolText` 同时持有 `Text` 与各上限常量），否则下次改正文没人会想起预算。

## 26. MSBuild：`<InternalsVisibleTo>` / `<AssemblyAttribute>` 是 **Item**，放 `PropertyGroup` 里必报 MSB4066（2026-09-15，V4.2）

- **现象**：为让「CLI 面」可测（I17），往 `AgentRuntime.Cli.csproj` 里加 `<InternalsVisibleTo Include="AgentRuntime.Tests" />`，直接放在 `<PropertyGroup>` 里 ⇒ `MSB4066: 无法识别元素 <InternalsVisibleTo> 中的特性"Include"`；换成 `<AssemblyAttribute Include="System.Runtime.CompilerServices.InternalsVisibleToAttribute">` 放在 PropertyGroup 里 ⇒ 同一个错。
- **原因**：这两个都是 **Item**（必须在 `<ItemGroup>` 里）；`PropertyGroup` 只认属性。错误话术（"特性 Include 无法识别"）**指向的是特性名，不是位置**，很容易往错方向找。
- **顺带**：XML 注释里**不能出现 `--`**（`MSB4025: An XML comment cannot contain '--'`）——`--tail-show` 这种命令行写法直接写进注释就炸。
- **处理**：`InternalsVisibleTo` 用 `AssemblyAttribute` 形式放进 `ItemGroup`；注释里把 `--tail-show` 写成 `tail 四条命令`。
- **通用化**：改 `.csproj` 时把 Item / Property 分清；**别在 XML 注释里写 CLI 选项原文**。CLI 面能用 `InternalsVisibleTo` 直接单测，比手感好得多 —— 代价只是一行 ItemGroup。

## 27. `dotnet run ... --nologo` 会把 `--nologo` **透传给应用**，未识别选项被当成「用户消息」（2026-09-15，V4.2，真机踩到）

- **现象**：`dotnet run --project src/AgentRuntime.Cli -v q --nologo -- --config X --tail "…"`：本意是「设一次白板就退出」，结果**真的向模型发起了对话**（`--nologo` 不是 `dotnet run` 的选项，落进了应用参数），而且模型回复末尾的自报被 `CurrentTailModule` 解析后**覆盖了白板** —— 白板里出现了模型自己写的「当前任务 / 待办 / 状态」三行。
- **原因**：`Program` 的 `default:` 分支把**任何未识别参数**当作本轮用户消息（裸词 = 消息，这是有意的便利设计）；`--xxx` 形式的未知选项**没有**被拦成 usage 错误 ⇒ 静默变成一次付费调用。
- **处理**：① 本机实验脚本改为**直接跑已构建的 dll**（`dotnet <path>/AgentRuntime.Cli.dll …`），彻底避开 `dotnet run` 的参数转发；② 白板操作与模型调用**分开跑**，看到 `[尾部] 已人工覆盖…` 才继续；③ 把这次意外当作管线证据入账（自报 → 解析 → 覆盖存储区，端到端真的通了）。
- **通用化**：① 命令行工具里「未识别的 `--xxx`」应当**报 usage 错**，不能等同裸词消息（否则每个手误都是一次真机调用）；② 跑真机的脚本**只跑 dll，不跑 `dotnet run`**，参数转发层越少越好。

## 28. 真机冷启动自检：**夹具逐字节相同 ⇒ 上一轮 run 已经预热**，第 1 轮 cached 不为 0（2026-09-15，V4.2）

- **现象**：同一套夹具（同 SEED、同 salt）连跑两次 V4.2 对照，第二次 A 组 turn 1 直接 `cached=2432`（第一次是 0/128）—— 数据看上去「缓存凭空出现」；「冷启动自检」（第 1 轮必须 cached = 0）直接把两轮数据都判成废数据。
- **原因**：上游前缀缓存是**跨进程、跨天**的服务端缓存；夹具字节没变 ⇒ 前缀就是同一段 ⇒ 第二次天然命中。#11 只说了「变体共用 salt」会在**同一次 run 内**互相预热，**没覆盖「跨 run」**这层。
- **处理**：给尺子加 `--seed`，salt 随种子变（`实验变体盐：{group}-{seed}`）⇒ 换种子 = 语料逐字节全变 = **真冷启动**；再跑一次，四组 turn 1 齐刷刷 `cached=128`（那 128 = 跨 run/跨组都逐字节相同的**协议区头 2 个 64 块** —— 它本来就该命中，是「协议区稳定」的正面证据）。
- **通用化**：① 冷启动自检的**前提**是「这段前缀从未上过网」——只保证「组间隔离」不够，必须让**夹具随 run 变化**（种子进 salt）；② 冷启动时若仍有一小块命中，先对一下那块的**字节来源**（是不是本来就不该变的协议区 / 系统提示），别一概当污染。
## 29. 量「模型遵从率」必须先设计**对的夹具**；测不出来时先怀疑量法（2026-09-15，V4.2，真机踩到 + 已纠正）

- **现象**：协议第 4 条写 `optional` 时，`[TAIL]` 自报率 **0/4**。改成**条件强制**后，**沿用同一夹具**又测出 **0/4**（换 `deepseek-v4-pro` 也 0/4）—— 一度被当成「模型不配合」。
- **根因（两层，都在夹具不在模型）**：① **探针题写「只回代号」**，与「末尾再加一段」**指令冲突**（实测规律：冲突时**用户那句话永远赢**）；② 更致命：老夹具里**任务/待办根本没变** ⇒ 按协议第 5 条「无变化可省略」，**省略本来就合规** ⇒ 该夹具**区分不了**「不遵守」与「正确省略」。
- **处理（已做，结论反转）**：另建量表 `benchmark/tools/v42-tail-compliance.py`（**任务每轮真的变化** + 探针问「现在在做什么/下一步」**不抢指令**）：
  - **B 组（纯协议驱动，用户消息不提 [TAIL]）自报率 4/4 = 100%**；X 组（显式要求）同为 100%；
  - 白板内容还**真的在累积**：当前任务 / 待办 / 遗留 / 暂停 / 已完成，逐轮跨任务传递；
  - 同一模型（flash）在旧夹具里是 0/4 ⇒ **差异 100% 来自夹具设计**。
- **通用化**：① 量「遵从率」必须让**条件真的触发**（否则测的是「会不会正确省略」）；② 探针**不得**与被测协议抢指令；③ **测不出来时先怀疑量法，再怀疑被测物**——否则会把「自己没给条件」写成「模型不行」（本次差点给协议打错刑）。

## 30. 协议允许一个回复带**多块**自报时，单块解析器会把别人的正文吞进来（2026-09-15，V4.3，设计时就撞上）

- **现象（推演 + 测试钉住）**：V4.3 的协议第 4 条允许模型在同一个回复里同时输出 `[TAIL]`（白板）与 `[DRAFT]`（草稿）。两个模块各自解析**同一份回复**：R4 的 `TryParseReport` 从下行找到最后一个 `[TAIL]` 头后**一路吃到结尾**，于是草稿块的 `[DRAFT]` 行与正文都被当成白板的行（白板被污染 / 或被判超限而整轮丢弃）；反过来先解析草稿也一样吞白板。
- **处理**：两个 `TryParseReport` 都加**分节符**——向下逐行时**遇到下一个块头即停**（块头清单 = `ProtocolText.ReportBlockHeaders` = `[FOCUS]` / `[TAIL]` / `[DRAFT]`，唯一声明处）；测试 `Draft_ParseStopsAtNextBlockHeader` 把「白板不吞草稿、草稿不吞自报行、两种次序都对」钉住。
- **为何以前的代码没暴露**：V4.2 的协议只定义了**一块**（`[TAIL]`），「吃到结尾」在那个世界里是对的；**协议一多块，解析就不再是「找头 + 吃尾」而是「找头 + 找尾」**。
- **通用化**：给文本协议加**第二种块**时，必须同时定义**分节符**；否则每个解析器都默默把下一块的正文当成自己的（不报错、只是内容错位 —— 最难发现的那类错）。

## 31. 用**一个数字**兼做「区的编号」与「区的先后次序」，重排一次就表达不出来（2026-09-15，V4.3）

- **现象**：V4.1/V4.2 里 `DynamicRank` 既是记号（R2=2 / R3=3 / R4=4 / R5=5）又直接拿来比大小（`rank < lastRank` ⇒ 违序）。V4.3 要把 **R3 排到最后**（R2 → R4 → R5 → R3）—— 但 R3 的编号永远是 3，无论怎么排序都比 R4(4)/R5(5) 小 ⇒ **闸门根本无法表达新规范序**，只能靠叫「位置」的另一个函数。
- **处理**：**拆开两个概念**：`DynamicRank`（区编号，只用于话术/身份）+ `DynamicPosition`（规范序下标，只用于比较）；规范序本身只在一个数组里声明一次（`DynamicSequence = [RankStream, RankTail, RankDraft, RankFocus]`），话术引 `DynamicSequenceText`。
- **通用化**：**身份**（是几号区）与**策略**（排第几）是两件事，第一次重排策略时就会打架；两者从一开始就分开声明，重排就只是改一个数组。


## 32. md 知识库里混进源正文标题 ⇒ 一次解析出 96 个「技能」（2026-09-16，skill-repo）

- **现象**：源技能正文本来就有 `## 适用` / `### M1. …` 这类标题；第一版解析器把「任意 `## X`」当技能头 ⇒ 一次解析出 **96 个"技能"**。
- **处理**：只有「**下一行是 `<!-- skill: … -->` 标记**」的 `## X` 才算技能头（标记是唯一判据）。
- **通用化**：用**扁平文本**承载**层级结构**时，标题级别不是身份；要一个**显式标记**当身份（否则用户正文里的同级标题会冒充结构）。

## 33. L3 条正文的**前后换行**与**分隔空行**歧义 ⇒ 往返只能到 21/27（2026-09-16）

- **现象**：条正文常自带前导/尾随空行；靠启发式（"分隔空行不算内容"）往返只到 21/27，写死规则反而 0/27。
- **处理**：在条首加一行 **`<!--wrap: lead trail-->` 显式记录前后换行数** ⇒ 27/27 逐字节。
- **通用化**：**往返（round-trip）不允许靠猜**；把「不可见的差异」**显式记进载体**，让重放是确定性算术而不是启发式。

## 34. 缓存按「源 sha」只保留**最后一次**响应 ⇒ 换提示词后回切要重花钱（2026-09-16）

- **现象**：以为"回切旧口径走缓存、0 调用"，实际缓存只留最新一条 ⇒ 回切也要重抽（1 次调用）。
- **通用化**：**别把"缓存"当成"历史"**——缓存是加速器（可被覆盖），历史要显式另存（如原始响应归档）；引用缓存做的**成本承诺**必须写成"预期"而不是"保证"。

## 35. 子会话跑长任务时，**中途发的规格变更只会排队**（2026-09-16，编排）

- **现象**：一次性发了 3 条规格修正（阈值 10%→15%、双条件、版本命名），子会话整轮跑完才可能看到 ⇒ 产出按**旧规格**成形（今天两次）。
- **处理**：要么**等交卷再发**下一条规格，要么**接受重跑**；并**在产物里记录"裁定时的规格值"**（阈值/上限等），事后一眼看出哪批是按旧口径做的。

## 36. 崩溃留下的**孤儿版本文件**：用「指纹重算」抓，不用眼看（2026-09-16）

- **现象**：进程崩在写版本与记账之间 ⇒ `versions/` 里多出一个**账本不认识**的快照。
- **处理**：`--check` 的「**版本完整性（逐版本指纹重算）**」把它抓出来；本次是崩溃产物（非用户数据），人工清理。
- **通用化**：多文件状态（md + 版本 + 账本）必须有**交叉校验**，否则"少记一笔"永远静默。

## 37. 交接文件名前导编号重复（两个「04」）⇒ `H-<nn>-###` 会撞 id（2026-09-16，知识库统一）

- **现象**：`workspace/handoff/` 里同时存在 `04-NodeMesh与IconForge-同步通知-给001.md` 与 `04-授权系统设计.md` —— 前导编号都是 `04`。按「交接 id = `H-<nn>-###`（nn = 文件名前导数字）」派生时，两个文件的第 1 条都想要 `H-04-001` ⇒ id 不唯一。
- **处理**：id 的 `nn` **不允许**只取裸前导数字当唯一键。派生器按**文件名排序**处理，同号冲突时给**后续文件**加小写字母后缀（第一个保持裸号 `04`，第二个 `04a`），保证唯一 + 确定 + 不静默。工具 `l3-unify.py` 已实现并落账本。
- **通用化**：**文件名前导编号不是唯一键**，不能直接当 id 片段；任何「从文件名提取的短码」进 id 前必须先做**冲突自检**（重复 ⇒ 确定性消歧或显式报错），否则两个文件的内容会互相覆盖对方的地址。

## 38. 坑集节号有非整数（`1.5`），且「N 条」计数与 `## ` 标题数不符 ⇒ 用节号当 id 会撞/漏（2026-09-16，知识库统一）

- **现象**：`PITFALLS.md` 有 `## 1.` / `## 1.5` / `## 2.` … `## 36.`。口头说「36 条」，实际 `## ` 标题是 **37 个**（`1.5` 是第 2 个）。若拿节号当 id：`1.5` 无法映射成 3 位整数，且 `## 2.` 之后所有号都偏移 1。
- **处理**：id 的 `<NNN>` 取**文档序**（1-based 序号，第 1 条 = `001`，与 skill `S-<short>-<seq>` 同构）；「人类编号」（1. / 1.5 / 2. …）**只进 `title`**，不进 id。这样 id 唯一、稳定、与节号解耦；计数以**派生器实际解析到的条数**为准，不以口头宣称的「N 条」为准。
- **通用化**：**人类给条目标号 ≠ 机器地址**。手写编号会插入非整数（1.5）、会漏重号，不能当稳定 id；id 一律用文档序，人类标签放 `title`；「一共多少条」以**派生结果**为准（可复算），不信嘴说。

## 39. 演示/转录里混入**墙钟时间**（开销 ms）⇒ 重跑 `demo.sh` 不是逐字节相同（2026-09-16，TUI 演示）

- **现象**：`demo.sh` 重跑后，`frame-96x30.txt` / `transcript.txt` 里的「开销 X.Xms」「总ms」会漂（如 6.6ms → 6.4ms）；但「字节 / 指纹 / 版本 / 合计」这些列是**确定性**的（协议文本不变 ⇒ 逐字节不变）。
- **处理**：帧/转录里凡是**墙钟 / 随机 / 环境值**（`开销ms`、`总ms`）重跑都会漂；对「逐字节一致」只锁**确定性部分**（字节 / 指纹 / 版本 / 合计），不比墙钟数字（或先把墙钟数字归一化再 diff）。
- **通用化**：**「逐字节一致」只对确定性产物成立**；产物里一旦混入墙钟/随机/环境值，重跑就不是逐字节相同。要在产物里把「确定性部分」与「墙钟部分」分开标注，diff 时只锁确定性部分。

## 40. 验证闸门时的「量法假阴性」三连（2026-09-16，验收）

- **现象**：同一晚三次判定「闸门没牙」，**全是量法错**：① `build-skill-repo.py --check` 少给必填 `--out` ⇒ argparse 早退，**根本没跑到闸门**（输出里"没有 FAIL"≠ 闸门通过）；② 负例改的是 `/tmp` 副本，而该检查按**账本记录的源路径**读源 ⇒ 改的是工具根本不读的文件；③ 把**软提示段**（含 `<user>` 的标记行）的行数当成了**硬红线命中数**。
- **处理**：负例必须改「**工具真正会读的那个对象**」（先看清解析入口再动手）；命中数要**按区块**分别量（硬红线段 / 软提示段），别整文件 grep。
- **通用化**：**「没复现」≠「没有」** —— 测不出来先怀疑自己的量法（同 METHODOLOGY §十·22）。负例要能说清「改了哪个字节 / 谁读它 / 预期哪条闸门变红」。

## 41. 窄豁免必须 fail-closed（2026-09-16，发布扫描）

- **现象**：给 `knowledge.md` 的技能标记行做 `src:` 豁免，若用宽松匹配（只匹配 `src: ‹redacted›`）会把**正文里**的真本机路径一起放行。
- **处理**：豁免正则**整行严格匹配**（字段序 / 字段名 / enum 值全要对），任一不符即**不豁免**；必须跑两个例子钉住：① 打乱字段序 ⇒ 重新命中；② 干净目录 ⇒ `exit 0`。
- **通用化**：**豁免的默认方向是「不豁免」**；豁免范围要能用一句话说清，且被负例钉死。

## 42. 知识库正文的硬红线：元数据豁免 ≠ 可发布（2026-09-16，发布扫描）

- **现象**：以为「豁免 `src:` 标记行」就能发布 `knowledge.md`；实测正文仍有 **10 处**硬红线：内网 SVN 地址 + 账号名、`‹redacted›` 路径、4 处真本机路径、2 处 `<home>` 占位示例、**2 处网盘地址**。
- **处理**：**元数据行与正文行分开判**；发布前**真跑** `tools/pre-publish-scan.sh` 并把命中**按类别**列清。收口二选一：正文改主机中性写法（`<workspace>` / `svn://<host>/…` / `<网盘地址>`，需记账写入 + 晋级新版本）或该文件**不进发布集**。
- **通用化**：**「不含凭据」不等于「可对外」**；只有跑过扫描、且命中逐条解释过，才配说「可发布」。

## 43. 文本协议的块头必须**在行首**：行中间的 `[TOOL]` 既不算批准、也不算调用（2026-09-16，工具面）

- **现象**：写测试时造了 `"好的，我已批准：[TOOL] write {…}"`，期望「模型自批不算 ⇒ 拒绝」。实际**连解析都没发生**（无 `TOOL_DENIED` 事件、无审批记录）—— 因为 `[TOOL]` 在行中间，`ToolReport` 的块头判据是「trim 后以块头开头」（与 `[FOCUS]` / `[TAIL]` / `[DRAFT]` / `[L3]` 同一条分节纪律）。
- **处理**：块头一律要求**行首**（家族一致）；测试把两条路都钉住：① 模型自称批准 + 行首 `[TOOL]` ⇒ **拒绝**；② 行中间的 `[TOOL]` ⇒ **不识别**（只有 user/agent 两条事件，文件确实没被写）。
- **通用化**：解析器的「严格」有**两个**失败面 —— 「拒了」是响的、「不认识」是**静的**。只测前者会漏掉后者；凡「不识别 ⇒ 什么都不发生」的路径，必须有一条断言**数事件条数**（证明确实什么都没发生，而不是「发生了但没记」）。

## 44. 零注入模块在确定性闸门里按 R2 类计次序 ⇒ 配置里放错位置会被判「动态区违序」（2026-09-16，工具面）

- **现象**：`tool` 模块 `ContributeAsync` 一个字节都不加（零注入），但 `DeterminismGate.DynamicPosition` 按「不是 R3/R4/R5 ⇒ R2」归类 ⇒ 配置写成 `["append-stream","focus","tool"]` 会被判「R2 出现在 R3 之后」而**报错**（闸门故意只报错、不纠正）。
- **处理**：`tool` 写在 `append-stream` 之后、`current-tail`/`dynamic-draft`/`focus` **之前**；这条纪律写进 `KnownModules` 的注释与 `DESIGN-TOOL-FACE.md` §七（配置的人能看见）。
- **通用化**：新增模块时除了「注入什么」，还要问一句「**它在动态规范序里属于哪一类**」；「零注入」不等于「不占位置」—— 位置是按**类**算的，不是按字节算的。

## 45. 新增配置段当天就要补进 `RuntimeHost.BuildModules` 的 `effective`（PITFALLS #14 的重演）（2026-09-16，工具面）

- **现象**：V6 加的 `skill`（L3 地址表路径）没被抄进 `RuntimeHost.BuildModules` 里手抄的那份 `effective` 配置 ⇒ 一旦用 `--modules` / `--bare` 覆盖模块列表，`skill.repo` 被**静默丢掉**（「按 id 装载」无声失效）。
- **处理**：补上一行 `Skill = config.Skill` + 注释；`BuildModules` 现在也把工具面的沙箱/闸门/账本透传给 `ModuleRegistry.Create`。
- **通用化**：`BuildModules` 是**手抄字段**的结构性风险点（#14 已记过一次）。判据很简单：`RuntimeConfiguration` 每加一个配置段，就在同一提交里回答「`BuildModules` 抄了吗」——漏了不报错、只是**静默丢配置**，这类 bug 只能靠纪律 + 代码里的同一处注释兜。

## 46. 审批面被右栏**截断**⇒「知情」变成假的（2026-09-16，TUI 审批）

- **现象**：`SplitRenderer` 对详细块一律 `TruncateTo(RightWidth)`。审批面一放进去就被截成 `[审批] 动作：write /private/tmp…` —— **真身路径看不全**，而审批的设计前提正是「人看见的就是真身」（S1/S2）。
- **处理**：`SplitFrame` 加 `DetailWrap` 开关，**只在待审批时打开**：详细块按 `RightWidth` **折行**（`TerminalText.Wrap`）而不是截断；行数变多没关系，那块本来就能滚。
- **通用化**：**「显示有预算」与「审批必须完整」是两条冲突的规则**，要显式分优先级 —— 凡是「人要据此做决定」的内容（审批面 / 报错原文 / 完整路径），**折行永胜截断**；纯粹信息型的（状态表、目录）才允许省略。

## 47. 端到端夹具三连坑（按键 / 提示吃 stdin / 回显缺块头）（2026-09-16，CLI+TUI 审批 E2E）

三个都让「测试红了但产品是对的」，属于**量法错**（§十·31 同一族）：

1. **整段写入 PTY ⇒ TUI 收不到键**：一次性 `write(整行 + \r)`，TUI 的重绘节奏下按键丢失；**逐字符 + 小间隔**写入才稳（`for ch in msg: write(ch); sleep(0.01)`，最后单独发 `\r`）。
2. **CLI 先问收尾提示**：有未收尾内容时 CLI 会在跑轮次**之前**问「现在就收尾吗？[y/N]」，它先吃掉 stdin 的 `y` ⇒ 审批面永远等不到答复。E2E 驱动必须按**提示顺序**应答（先 `n` 给收尾，再给审批）。
3. **mock 回显的两个坑**：① 回显内容**没有行首块头** ⇒ 解析器不认（`#43`），要在夹具里补成 `[TOOL] …`；② 夹具取的是**第一条** user 消息，多轮时会拿**旧目标** ⇒ 改成「取最后一条以 `@tool ` 开头的 user 消息」，并且每轮跑前清流。
- **通用化**：E2E 夹具**本身**要当被测物一样审：**谁先读 stdin / 输入怎么切分 / 状态怎么跨轮残留**，三处各能制造一次假红。

## 48. 量法错三连：取证据的通道被"吞"掉（2026-09-16，SVN 操作）

- **现象**：一小时内三次把"正确"判成"错误"：
  1. `cmd | tail -N` 之后量 `$?` ⇒ 量到的是 **tail** 的退出码（`build-frozen-corpus.py --check` 明明 exit 1，我读到 0）；
  2. **`svn log -l 1` 在「工作副本路径」上报的是 BASE，不是 HEAD** ⇒ 刚提交的 r581 看不见，以为"没提交成功"；
  3. `svn cat <工作副本路径>` 在**树冲突路径**上会失败，被 `2>/dev/null` 吞掉 ⇒ 我拿到的"仓库版"其实是**空文件**，于是判"DIFFER"（其实两边逐字节相同）。
- **处理**：① 闸门判定**直接跑、不接管道**；② 判提交成败用 `svn update` 后的版本号或 `svn log -r HEAD`；③ 取仓库版走 **URL**（`svn cat <URL>/…`），并且**不要**给可能失败的通道加 `2>/dev/null`。
- **通用化**：**关掉静默、指定权威来源** ——「静默失败 + 我以为它表示别的意思」是假结论的标准配方（`METHODOLOGY` §十·35）。

## 49. `commit` 后紧跟 `update` 的 add/add **树冲突**（内容其实相同）（2026-09-16，SVN 操作）

- **现象**：`svn commit` 成功（r582 就是本次提交）后立刻 `svn update`，两个**新加文件**报 `local file add, incoming file add upon update` 树冲突，`svn status` 显示 `C`；随后文件被标 `R`（替换），像是要再提交一次。
- **判定**：先证实**内容是否相同** —— 用 URL 取 r582 的版本与本地 `cmp`（本次 **IDENTICAL**）⇒ 属**假冲突**，不丢任何东西。
- **处置**：`svn resolve --accept=working <paths>`（**该类冲突不适用 `--accept=theirs-full`**，会报 `W195024`）→ 再 `svn revert <paths>` 回基线（内容相同 ⇒ 不产生空提交）。
- **预防**：**`commit` 之后先 `svn status`，再 `svn update`**；出现 add/add 且内容一致 ⇒ 按上面两步收口，**不要**顺手再提交一次。


## 50. `svn commit -F <文件>` **不给路径** ⇒ 提交工作副本里**所有**改动（2026-09-16，SVN 操作）

- **现象**：用 `svn commit -F /tmp/msg.txt` 提交"must-ask 档"这一件事，结果把**同一次工作里的另一件事**（Stage 0 文档 + `shadow-compose.py` 修复）一起提交进了同一个 revision（r588）⇒ 提交信息与实际内容不符。
- **根因**：`-F` 只提供**提交信息**；**提交哪些路径**是另一件事 —— 不给路径就是"工作副本里所有已改动/已 `add` 的"。
- **处置**：`svn propset svn:log --revprop -r 588 …` 试改提交信息 ⇒ **被拒**（`E175008`：仓库未开 `pre-revprop-change` 钩子）⇒ **改不回来**，只能在后续提交/公告板里说明。
- **预防**（两条一起用）：① **提交信息一律 `-F <文件>`**；② **要提交哪些路径必须显式列出**（尤其当天有第二件事时）。**忘了就等于没有"按事提交"这回事。**

## 51. 多行任务喂进 CLI/TUI 的 REPL ⇒ 一个任务被拆成 **N 轮**（2026-09-16，Stage 0 夹具）

- **现象**：把一份 7 行的任务描述整段喂进 `--ui plain` 的 REPL，事件流出现 **14 条**（7 问 7 答）、`/hits` 记 7 轮 —— 看起来像"模型跑了 7 轮"，其实是**夹具**把每一行当了一次用户输入。
- **根因**：REPL 是**面向行**的（一行 = 一次输入），对多行文本没有"整段提交"语义。
- **预防**：夹具喂任务前**压成单行**（结构用 `①②③` 或 `；` 串起来）；判"跑了几轮"**只信事件流**，不信终端看起来像几轮。

## 52. 夹具的"安静判据"太短 ⇒ 把长回复截成 **0 事件**（假失败）（2026-09-16，Stage 1 夹具）

- **现象**：第一版驱动设"25 秒没有新输出就算跑完"，结果一次真机运行报 **0 事件 / 0 轮** —— 看着像"什么都没发生"，实际是**模型还在生成**（那一轮 provider 耗时 **91.3 s**、completion 18k token）就被夹具打断。
- **根因**：把"安静"当成了"结束"；而**长回复的安静期可以远大于普通一轮**。
- **预防**：安静阈值按**最长一轮**留量（本次 25 s → 100 s）；并且**报告里要区分"真的没产物"与"被我打断"**（本次若只看 0 事件就会写出完全错的结论）。

## 53. 结构变化后，**脚本/夹具/配置里还指着旧路径**（"三处同步"的第三例）（2026-09-16）

- **现象**（同日两例）：① `tools/shadow-compose.py` 的语料清单还写着改名前的 `OWNER.md` / `METHODOLOGY.md` / `memory/INDEX.md` ⇒ 对照表里出现 3 行「（不存在）」；② `tools/skill-repo/selftest-gates.sh` 仍 `cp -R ~/.openclaw/workspace/skills/<skill>`，而本机 `skills/` 已在「工作区侧收敛」时**卸载** ⇒ 负测直接跑不起来。
- **根因**：#36 说的"三处同步"只覆盖了 spec / 账本 / 文档 —— **引用点还包括脚本的输入清单、负测夹具的输入路径、配置里的地址表**。
- **预防**：改名/下线任何目录后，**grep 全仓引用点**再收工；把"引用点清单"当成"三处同步"的第四处。

## 54. 实现有了、**配置没启用** ⇒ 功能空转（2026-09-16，知识库接线）

- **现象**：`[L3]` 自报 → 按 id 装载 L3 的**实现早已在**（V6），但三个配置都没有 `skill.repo` ⇒ 接线**整体空转**；待办里被记成"尚未实现"。
- **根因**：代码刻意「**不猜默认路径**」（好事），代价是"没配就没接"这件事**不会报错**（静默）。
- **预防**：收尾核查"某能力到底通没通"时，**先看配置有没有启用**，再看实现有没有写 —— 否则会把"没开开关"误记成"没做功能"。**反例**：本次差点按"重新实现"的方式去做 #5。

## 55. B 档构建**不加 `--out`** ⇒ 把 A 档产物**覆盖成 B 档内容**（2026-09-16，语料构建）

- **现象**：跑 `--tier B`（图省事没带 `--out`）之后，`--check --tier A` 报「产物与重算不一致 + 内容已变但版本未 bump」；核对发现 `frozen-private/knowledge/global.md` **多出了整节「五、项目文档索引（L3）」** = A 档产物被 B 档内容覆盖（98,966 B vs A 应有的 85,226 B）。
- **根因**：`--out` 的**默认值是 A 档目录**（`project/frozen-private`），`--tier` **不会**自动改输出目录 —— 文档里的正确写法是 `--tier B --out frozen-private-b`。
- **处置**：`--tier A`（默认 out）重建 → `--tier B --out frozen-private-b` 重建 → 两档分别 `--check`（A `9d3c27be68c98cba…` / B `38b3d16f91217536…`，均 exit 0）。
- **预防**：**A/B 两档的 `--out` 一律显式写全**；`--check` 也要带对应的 `--out`（不然校验的是默认目录，结论会串档）。**闸门之所以能报出来，正是因为它比的是「产物 vs 重算」，不是「文件在不在」**。

## 56. 「文档说没做」≠ 真没做 ⇒ 白跑一轮（幻影待办）（2026-09-16，[WB] `[L3]`）

- **现象**：待办 #2 写「`[L3]` 协议正文仍未写 ⇒ 升 v8」，依据是某设计文档里的一句「仍未写」；实际**逐字核对 `svn cat -r 570/-r 600` 后，该条款自 r570（协议 v6）就在正文里**，v6→v7 只补了 `[TOOL]`。
- **根因**：把「**这一轮的增量**只有 X」读成了「**整条**没写」；进度判断依赖**文档自述**而不是**产物**。
- **预防**：判「做没做」只认**产物与仓库**（`svn cat` / `grep` / 真机跑一遍）；文档里的自述一律当**线索**，不当结论。

## 57. 认证/权限类改动：**先分辨「声明层」还是「边界」**（2026-09-16，安全网关）

- **现象**：判定链写得再全，只要 **Agent 进程自己就能伸手**（自己实现 write/exec），「必须经过网关」就只是**纪律**，不是**边界** —— Agent 完全可以不走网关直接动文件。
- **根因**：把「我们代码里会检查」当成了「它做不到」。**判定者与被执行者同域 ⇒ 没有边界**。
- **预防**：任何安全改动先回答两问：① 谁能绕过它？② 绕过需要什么权限？—— 答不出就别称「边界」，老老实实叫「声明层」，并写明**硬边界仍未落地**。

## 58. macOS 系统级安装的三处「看起来没事」（2026-09-16，T3 launchd 服务）

- ① `mktemp -d` 建出来的是 **root 0700** ⇒ 再用 `sudo -u #<uid>` **降权**去写，写不进去（表现：对每个文件重试 10 次）。
- ② launchd 只按 `UserName` 换 uid，**gid 仍沿用 root** ⇒ 必须显式 `<key>GroupName</key>`，否则服务建的文件落在 `wheel`。
- ③ **macOS 新文件/套接字继承「父目录的组」**（不是进程的 egid）⇒ 想让某 uid 连上套接字，得把**父目录**的组设成它所属的组。
- 另：`File.Exists` 对**套接字文件**返回 `false` ⇒ 「存在就删」的陈旧清理**永远不生效**，重启直接 `EADDRINUSE`（症状：服务 `runs=2` 但 socket 不出现）。
- **预防**：系统级脚本的自检判据要写成「**真的能连上/能用**」，不要写成「文件在不在」。

## 59. 状态落点：**判不出基准位置就报错，绝不回退到 cwd**（2026-09-16，预授权账本）

- **现象**：`HOME` 指向不存在的位置时，`SpecialFolder.UserProfile` 返回**空串**，`Path.Combine("", ".agentruntime", …)` 变成**相对路径** ⇒ 预授权账本被写进了**仓库目录**（差点入库）。
- **预防**：① 判不出基准位置 ⇒ **抛错**（fail-closed）；② 测试别指望 `HOME=` 隔离（macOS 上 `GetFolderPath` 不认进程 `HOME`）⇒ 用**显式落点参数**；③ 跑完**核对外面有没有多出目录**。

## 60. 夹具会「吃掉」它要测的东西：多轮 + 回显前缀（2026-09-16，九例夹具）

- **现象**：mock provider 回显会给回复加 `[mock] 收到：` 前缀；单轮夹具靠**消息前置换行**让 `[TOOL]` 落在行首；而 `--chat` 的 `ReadLine()` **会吃掉那个换行** ⇒ 块头被挤到行中 ⇒ 解析不到工具（于是测的其实是「没有工具」的路径）。
- **预防**：夹具要断言「**被测量真的发生了**」（本处：事件流里真有 ToolResult）；多轮场景改用**同一 Runner 两轮**的集成测试，别硬凑 PTY 多轮。

## 61. 实测数值存根：从 `KNOWLEDGE.md` §十 撤下来的数字（2026-09-16 精简时下沉）

> **为什么要存**：§十 已精简为**法则骨架**（18,357 → 11,043 B，−39.8%），判据/实测数值按纪律**下沉到正文家**（本文件）。
> **怎么用**：要复核某条法则时按编号回查这里的数字；§十 正文只留法则，不再重复这些数。
> **注意**：这些是**当时的实测**，不是永久常数——复现前先看对应 skill 的 L1/L2 与更晚的坑条。

| §十 条 | 撤下来的实测数值 |
|---|---|
| 6 | 前缀缓存按 **64 token 块**对齐；稳定前缀 + 只追加 ⇒ 命中 **98.4%**、单轮成本 **−78.5%**；**动态内容前置 ⇒ 命中归零、成本 4.7×** |
| 7 | 尾部追加省 **78.3%**；**改头部一个字节 ⇒ 命中 0、成本 4.6×**；`knowledge` 段 v4→v5 后冷启动仍命中 **6,400 token**（= 未改动的 `rules` 段） |
| 12 | 估算口径 `字符 ÷ 1.88` **实测偏差 0.3%**；文件头 `<!-- frozen: version=N -->` 被剥离 ⇒ **×0.996 不放大** |
| 13 | 索引化实测 **913 KB → 133.5 KB（↓85%）** |
| 15 | 跨进程续接真机 **`cached 29,312/29,542 = 99.2%`** |
| 20 | 底座文本 **≈8.8k** + 工具 schema **≈14.9k**；注入四件套 **≈3.0k**；技能目录 **≈2.0k**；**中文 md ≈3.2~3.6 字节/token**（旧的 ÷1.88 高估近一倍，弃用） |
| 22 | 同一份协议、同一个模型：**旧夹具 0/4 ⇒ 新夹具 100%** —— 差异 **100% 来自夹具设计** |
| 25 | 27 技能 / 200 KB / 434 条 ⇒ 常驻 4.6+18.1 KB ≈ **7,001 token**，单条取用 ≈ **1~1.5 KB**；粒度：fine（83 条 / 266 B）✗ · coarse（22 条 / 1,007 B）～ · **功能闭环（43 条 / 515 B）✓** |
| 26 | `svn-workflow`：索引多花 ~**550 token** 常驻，**每次使用省 ~1,700 token**（用一次回本 3 倍）；索引化 4 → **12**；全库常驻 **7,001 token**；每次使用省 **~70%**；换预算**一条命令、0 调用** |
| 30 | 三处闸门实证：TUI 帧（注入非墙钟差异 ⇒ `exit 1` + diff）· 全库汇总（截断 `_l1` ⇒ `1 ≠ 27` FAIL）· 源指纹（真归档改末字节 ⇒ 4 项 FAIL + `exit 1`，还原后 sha 复原） |
| 31 | 一晚三次「闸门没牙」判定，**三次都是量法错**：必填参数没给（脚本早退）· 负例改到工具**不读**的 `/tmp` 副本 · 把**软提示段**命中当硬红线 |

> **对照口径**（与 `handoff/15` 同一套）：中文 md **3.24 B/token**（22.7 KB 常驻 ≈ 7,001 token 实测）。

## 62. 只验「文件在不在」的闸门 = 假绿（2026-09-16，skill-repo 全库汇总）

- **现象**：`build-skill-repo.py --only <少数技能>` 时 `write_repo_level` 只拿**本次处理的 metas** 写全库汇总 `_l1/_l2` ⇒ 30 技能被压成 3 行、188 问压成 1 问；而 `--check` **只验「文件在不在」** ⇒ **假绿**（静默丢 30 个技能的常驻 L1）。
- **根治**：① 汇总**恒从全库账本**（`*/.meta.json`）重算，与 `--only` 无关；② **写与校验共用同一函数**（构造上不可能不一致）；③ `check` 升级为「与全库账本**重算逐字节**比对」。
- **通用判据**：**校验必须与写入同源**；只要还问「文件在不在」而不问「内容对不对」，就会假绿。

## 63. 新加的「路径」字段漏进唯一解析处 ⇒ 按 cwd 猜（2026-09-16，skill.resident）

- **现象**：`config.skill.resident` 是新字段，没进 `RuntimePaths` 的解析名单 ⇒ 相对路径**按 cwd 解析**；换个 cwd 直接 `DirectoryNotFoundException`（在 cwd 恰好等于配置目录时「碰巧对」）。
- **预防**：**新加任何路径字段 ⇒ 立刻在唯一解析处注册**（本处 `RuntimePaths.ResolveSkillResident`）；配置里的相对路径一律按**配置文件所在目录**解析，别依赖 cwd。

## 64. 长流程夹具三坑：只在新输出时判定 / 不复位世界 / 收尾提示吃 stdin（2026-09-16，真模型量表）

- **① 只在新输出时判定**：我的 PTY 夹具把「见审批面就点头」写在 `if 有新输出` 分支里 ⇒ **阻塞等输入的进程不再产生输出** ⇒ 夹具干等（实测卡 3 分钟无事件）。
  **修**：判定移到**每轮循环**都跑。
- **② 不复位世界**：上一轮的改动留在磁盘 ⇒ 模型读完发现「活已做完」而**合理地什么都不改**，被误判成「只读不改」。
  **修**：每轮跑前 `svn revert <目标>`，让「未完成」是事实。
- **③ 启动期的收尾确认先吃 stdin**：CLI 先问 `[收尾] 现在就收尾吗？[y/N]`（**`Console.ReadLine`，且是 `[y/N]` 不是 `(y/N)`**）⇒ 不答就卡死。
  **修**：夹具先答 `n`，再答审批。

## 65. 「看着有表 ≠ 表里有数」：正则漏字段 ⇒ 表空而闸门绿（2026-09-16，影子对照表）

- **现象**：分段明细行是 `· <区名> <段id> @ <版本> → …`，我的正则**漏了区名** ⇒ 明细全空、三条可比段全显示「（缺）」，而 `--check` **还是绿的**（因为「重算 == 文档」两边都空）。
- **预防**：闸门除了「重算 == 产物」，还要断言**关键字段非空**（本次已加：可比段在 [WB] 侧找不到 ⇒ FAIL）。

## 66. 改名（口径/块名）必须扫到「量表」与「夹具」两处（2026-09-16，[SKILL]→[L3]）

- **量表**：`v6-skill-compliance.py` 扫的是字面量 `[SKILL]` ⇒ **块名一改，量表永远量到 0**（静默量不到，不报错）。⇒ 改名要连**度量工具**一起扫。
- **夹具**：协议预算负例原先写死 `new string('x', 80)` ⇒ 预算从 340 放宽到 400 后**不再越界**，负例失效（「正例绿」≠「夹具有效」）。⇒ 负例要**按预算算超出量**（`DeclaredMaxTokens * CharsPerToken + 8`）。
- **通用**：改一处**名字/预算**，要把「谁在按名字/按数值找它」全部列出来（量表、夹具、文档、历史记录）——**历史记录保持原样**，活表面全部跟上。

## 67. `LC_ALL=en_US.UTF-8` 全局 export 会波及同 shell 的 bash 脚本（2026-09-16，selftest）

- **现象**：`$var` **紧跟全角字符**（如 `$got（`）时，UTF-8 locale 下 **macOS bash 3.2 把全角字节吞进变量名** ⇒ `unbound variable`。本机 `LANG` 为空（C locale）所以一直没暴露 —— 按 `svn-workflow` 的习惯 `export LC_ALL=en_US.UTF-8` 才踩到。
- **预防**：脚本里所有「变量紧跟全角字符」一律写 `${var}`；**别全局 export LC_ALL**（只在单条 svn 命令前缀上带）。
- **小坑**：`pathlib.Path.write_text(newline=)` 在 **Python 3.9 不存在** ⇒ 用 `io.open(p, "w", encoding="utf-8", newline="\n")`。

## 68. 切轮用错了分隔符 ⇒ 多轮规模**静默少算**（2026-09-16，G6 双跑采集器）

- **现象**：`--verbose` 的逐轮诊断行**只有第 1 轮带 `[turn N]` 前缀**，第 2 轮起是裸行。采集器按「前缀切轮」⇒ 后续轮**全部塌进第 1 轮**（后写覆盖前写），表里「轮数」显示 1、prompt 只算第 1 轮。**没有任何报错**。
- **为什么危险**：它长成「表里有数」的样子（数字看着合理），却把多轮任务的规模**系统性少算**（实测 T3 少算 4 轮的 3 轮）。
- **修**：**用每轮的终结行切轮** —— `finish_reason` 每轮必有一行，见到就开新轮（`tools/double-run.py::parse_transcript`）。
- **通用**：凡是「从文本日志里切分记录」，别依赖**只出现在第一条**的装饰（前缀/横幅）；要依赖**每条都必有**的字段。

## 69. 审批计数：「项」不是「次」，且不许按累积缓冲区的文本出现次数计（2026-09-16，G6 采集器审批列；同日复核更正）

- **原判（已更正）**：G6 首跑 T2 报「审批 2 次」而事件流里只有 **1 个 `ToolResult`**，当时判为「终端重绘 / 回显把同一提示串**又出现一次** ⇒ 过计」。
- **复核（2026-09-16，同任务重跑 + 终端全文逐行核对）**：`pty.openpty()` 后直接读 master 是**字节流**，这条路径里**没有屏幕重绘**。真相是**单位错** —— 审批面**逐项**问人：一次 `exec` 的授权面 = `[1/2] proc.exec` + `[2/2] net.request` ⇒ **2 个提示 / 1 次工具运行**。终端全文里 `[审批] 执行？(y/N)` 出现 2 次、`[审批] 已点头（逐项…）` 2 次、面首行（`需要人工点头`）1 次 —— 与 1 个 `ToolResult` **完全自洽**。**「2」不是过计，是把「项」当成了「次」。**
- **真隐患（不是计数错，是会改走向）**：`buf.count(MARK) > approvals` 这个写法是「累积缓冲区里出现过几次」—— 一旦真的重读 / 重放缓冲区，同一提示会被**再答一次**，多写的那个 `y` 会落到**下一个**提示上（例如收尾确认 `[收尾] 现在就收尾吗`）当答复 ⇒ 静默改变会话走向。
- **修（2026-09-16）**：`src/AgentRuntime.Cli/e2e-shadow-stage1.py` ①计数改**游标消费**（`buf.find(MARK, cursor)`，游标只前进、已扫过的字节永不重扫）；②收尾报**两个单位**：`审批 N 项 / M 批`（批 = 一次工具运行的授权面）—— `tools/double-run.py` 的 `审批` 列取到的仍是**项数**（口径已写进工具）；③断言按**批**做 —— `批 ≤ ToolResult + ToolDenied`，另加独立对账 `项 == CLI「已点头」行数`，违反即打印 `[告警]`（`approval_warnings()`，正例不报 + 两条负例均报，夹具已验证）。
- **别照抄的写法**：`审批数 <= 工具结果数 + 被拒数` 若按**项**去比就**恒假**（实测健康跑：2 项 vs 1 个结果）⇒ 报警变成狼来了。**比大小之前先对齐单位。**
- **预防**：计数一律按**事件 / 按新增片段**计（流里的 `ToolResult` / `ToolDenied`、结构体），不按**文本出现**计；报数**必须带单位**（项 / 批 / 次 / 条），单位不同的两个数不许并列比较。
- **通用**：和 #65、#70 同族 —— **闸门 / 计数器报的数，必须能说清它是从哪个可复算的来源数出来的、单位是什么**。

## 70. 「命中率」不是同一把尺子：跨任务共享前缀 ≠ 会话内多轮（2026-09-16，G6 双跑）

- **现象**：[WB] 三题连跑**共享同一冻结前缀** ⇒ 命中 99.2~99.6%；[OC] 每题是**新会话** ⇒ 命中 5%（单轮）/ 51%（2~3 轮）。
- **风险**：直接把这两个数放一起比 ⇒ 得出「[OC] 缓存差得多」的**反向结论**，其实两者**分母范围不同**（跨任务 vs 会话内）。
- **预防**：报命中**必须同时报它的分母范围**（跨任务 / 会话内 / 单轮），并把「不是同一把尺子」写在数字旁边；需要严格对照时，令两侧的**冷/热状态**先对齐（同冷启动或同预热）。
- **通用**：属 §十·31「量法错家族」——**先问『这个比值的分子分母各是什么范围』，再比大小**。

## 71. [WB] 侧「路径基准」有两处暗礁：相对路径按 **CLI 目录**解析、常驻知识里的项目根是 **[OC] 副本路径**（2026-09-16，G7 有限切换 D1 实测）

- **现象**（真机连撞）：① `read {"path":"projects/AgentRuntime/docs/PITFALLS.md"}` ⇒ 真身变成 `…/src/AgentRuntime.Cli/projects/…`（文件不存在）；② 模型按**它常驻知识里的项目根**去读 `~/.openclaw/workspace/projects/…` ⇒ 也不存在（真身在 SVN 工作副本 `…/Documents/AgentWorkFlow/software-company/projects/…`）。
- **根因**：工具面**不做路径围栏**（S1）⇒ 判「能不能改」的唯一入口是审批闸门，但**「从哪算起」仍得有个基准**：现在是**进程当前目录 = CLI 项目目录**（不是调用者的 cwd，也不是语料里说的那个「项目根」）；而 [WB] 常驻语料是从 [OC] 侧工作区剖出来的，里面的路径口径指向 `~/.openclaw/workspace`。
- **处置 / 预防**：① 任务文本给**绝对路径**（最稳），或从 CLI 目录起算；② 工具失败信息**必须带真身路径**（已做到 —— 模型靠它自纠，代价 1 轮）；③ **切过去之前**把 [WB] 语料里的路径口径对齐真身（或做等价映射）—— 这是**切换项**，不许靠模型猜（见 `docs/G7-DAILY-CHECKLIST.md` §三·3）。
- **通用**：同族 **#433/#434** —— 「路径从哪算起」是**口径**不是细节；凡「知识里说的位置」与「进程看到的位置」有两套，就一定会分裂，必须在**唯一解析处**钉死，并让失败信息**自证**。

## 72. 「空输出」不等于「没变化」：先看退出码与 stderr，再下结论（2026-09-17，G7 有限切换 D6 实测）

- **现象**（真机）：[WB] 首次跑 `svn status` **输出为空** —— 差点被读成「工作副本没改动」（真实情况是**那条命令自己失败了**；它随后修正写法才拿到真状态）。若按这个空结果继续，就会得出**反向结论**（「没改」→ 跳过核对 / 漏提交）。
- **为什么危险**：这是**静默失真**家族（§十·1 / #65 / #70）：命令失败**不一定会让你看见**，而「空」本身看起来很像一个合法结果。
- **预防**：① 任何命令的结论先看 **退出码 + stderr**，再看 stdout；② 关键判定**不用「看到空了就推定」**，而是让命令**自己证明**（例：`svn status` 空 ⇒ 再跑一次带 `--verbose`/区分「命令失败」与「无改动」的交叉检查；或像本工具族那样**断言有数**而非「在不在」，#62）；③ 取证 / 汇报类题面显式要求「**逐字回读**」（见清单 §三·10）。
- **未复现项（如实登记，不作结论）**：该跑还报「`--trust-server-cert-failures` 必须与 `--non-interactive` 同现，否则 `E205000`」；**在网关环境里两种写法都正常**（exit 0、输出相同）⇒ 登记为**待复现**，下次在 [WB] 的 exec 环境里两种写法对照。
- **通用**：**「我没看到」≠ 「没发生」**；凡是拿观测去支持结论的场合，观测本身要先自证（同族 #62/#65/#70）。

## 73. 发布线三条：`dotnet test` 不编 Benchmark · 打包清 `bin/obj` · 去隐私别碰冻结前缀（2026-09-17，v4.0 发布实测）

- **① `dotnet test` 不编 Benchmark 项目** ⇒ 只跑测试**漏掉语法错**（本轮真撞上：`CorpusLock.cs` 一处字符串拼接写成跨行、Benchmark 不被测试工程引用 ⇒ 没编到）。**口径**：验证一律 **先 `dotnet build`（整解决方案）再 `dotnet test`**。
- **② 打包发布物前先清 `bin/obj`**：在切片目录里编译过 ⇒ `cp -R` 把构建产物带进暂存区；而**发布扫描器按设计不看 `bin/obj`**（躲误报）⇒ **闸门不拦**。**口径**：打包脚本**先清再扫**；清完再复查暂存清单里有没有 `bin/`、`obj/`、`.e2e*`、`runs/`、`local-corpus/`。
- **③ 去隐私 / 去人名时别碰冻结前缀**：本想把发布集里的「作者」全扫（69 处 / 46 文件），其中命中 **`src/AgentRuntime.Core/Protocol/ProtocolText.cs`（协议正文 = 冻结前缀的一部分）** ⇒ **改一个字节 = 缓存整体归零**（违反 R14），而且那是本系统自己的用词。**口径**：批量替换前先问「这个文件在冻结前缀里吗」——**冻结区 / 协议正文 / 常驻知识一律不改**，只在**派生物**里换词（如公开坑集变体走生成器）。**已全部 revert**，只留该改的两类（人名 + 一处注释）。
- **通用**：三条都是「**动作对了、范围错了**」—— 与 #62（只验在不在）/ #72（空 ≠ 无）同族：**先划清边界（编什么、打什么包、动哪些文件），再动手**。

## 74. agent 改自己的宿主：审批面「先展开后批准」· `read` 的两层截断（2026-09-17，[WB] 首次自迭代实测）

- **① 首次 `edit` 被 `[TOOLDENIED]`：不是「没问」，是「项没展开 ⇒ y 无效 ⇒ 取键耗尽」**。闸门只在**每一项都有结论**时才算 Approved；而**长内容（决定型内容）必须先按 Enter 展开，按 y 才生效**（S3 知情同意）。闸门有**三条**路会记 `ApprovalActors.NonInteractive`：`!_interactive()` / 取键失败（无可读终端）/ **取键超过 `MaxKeysPerApproval = 64`**。本轮判据链：**第二次同通道、同闸门、只改了操作顺序就成功** ⇒ 前两条被证伪 ⇒ 只剩**取键超限**（未展开的项对 y 无反应，反复无效键耗尽配额）⇒ `Unknown` ⇒ fail-closed 拒。
  - **表象极具误导性**：`non-interactive` 这个字段名让人往「通道不是终端」上想（我一度据此断言 `Console.IsInputRedirected` 为真，**属错判**）。
  - **口径**：见到 `[TOOLDENIED]` **先别改架构**，先问「**这一项展开过没有**」；重试时**先 Enter、再 y**。
- **② `read` 的输出有**两层**截断，别拿一层当另一层**：返回头「400/632 行（上限 400 行）」是**行窗**（工具给的），正文尾「…（截断：仅前 8000 字符，共 14008 字符）」是**字符窗**（渲染给的）—— 两回事 ⇒ **头部行号 ≠ 实际可见量**。本轮真踩：宣称 `SplitView.cs` 1~400 行已读，实际可见约 240 行，`Render` / `ConversationLines` 段**从未进上下文**，差点漏掉需求 A 的落点。
  - **口径**：`read` 结果**先看末尾**有没有截断提示，再决定「读全了没」；要后段就 `offset` 接着读，别凭头部下结论。
  - ✅ **2026-09-22 已修**：`read` 改成**装到字符上限为止**（行窗与字符窗对齐）⇒ **头部行号 = 实际可见量**，且截断指针 = 视野的下一行。
- **③ 找类型别用 `find` 文件名**：**类名 ≠ 文件名**（`SplitFrame` + `SplitRenderer` 同在 `SplitView.cs`）⇒ `find -name 'SplitRenderer.cs'` 必然落空。**口径**：找类型用 `grep -rn 'class X'`。**同族**：**先读规格再调用** —— 本轮 `exec` 参数键写成 `cmd` 被拒（`ToolArgs` 的「未声明键即拒」），与文件名同属「凭印象猜名字」。
- **通用**：三条同族 —— **把「工具给的一行字」当成「事实结论」**（审批者字段 / 头部行号 / 文件名）。与 #62（只验在不在）/ #72（空 ≠ 无）/ #73（动作对了范围错了）同源：**先核边界与前提，再下结论**。

## 75. 未提交的自迭代产物 = 易碎品：他方的 `revert` 会**连坐**抹掉你（2026-09-17，[WB] 首次自迭代实测）

- **症状**：A 两处 `edit` 成功 + 回读核对通过 + 编译报绿 + 测试全绿 —— 十几分钟后发现**盘上源码已回原始**（`TerminalText.cs` 186 行 / `SplitView.cs` 632 行），**DLL 里查不到新方法**。
- **根因**：`svn log` 显示 **r676（01:57:58）** 是**另一会话**的「公开物去人名」——它做了**全树批量替换**（46 文件，含协议正文），自查发现会毁缓存 ⇒ **`revert` 全树**。该 `revert` **不认人**：把工作副本里**所有未提交改动**一并还原，**包括本端正在做的改动**。
- **铁证三对齐**（缺一不可）：① 全 `src` 树 mtime 同一分钟（`01:57`，= 批量写指纹）；② 编译产物 mtime 紧随其后（`01:57:18`）且**不含**新符号；③ 该目录 `svn status` **干净**。⇒ **「我只改了两个文件」解释不了「全树同时被写」**，必定是批量动作。
- **最阴的一处**：**「编译成功」也会骗人**。源被回退后重编，输出照样是「0 警告 0 错误」；区分点在**用时**（空转 0.73s vs 增量真编 2.21s）与**二进制符号**（`0` vs `1`）。⇒ **验证要落二进制，不看日志大字。**
- **连带修正**：事故前那批「编译 + 测试通过」的**证明力不成立**（跑的是旧代码）—— **错误的时间点会让正确的动作产生假的结论**。
- **口径**：
  1. **自迭代产物必须尽快 commit** —— 未提交的改动 = 借来的，他方一个全树 `revert` 就归零；
  2. **多人/多 agent 共用一个工作副本时，别做全树批量写/revert**；要做，先在 `BULLETIN.md` 打招呼（铁则「同一时间只在一台机器改同一文件」防的就是这个）；
  3. 发现「刚改的东西不见了」⇒ **先查 `svn log` 那几分钟有没有全树级提交**，别怀疑自己的编辑没生效。
- **通用**：与 #62（只验在不在）/ #73（动作对了范围错了）同族 —— **先划边界，再动手**；本条补上后半句：**划完边界，赶紧落盘入库**。

---

## 76. 「能编译自己」≠「新代码生效」；而覆盖运行中的 dll 有真实风险（2026-09-17，作者问「TUI 正在终端里跑，真能自己编译自己吗」）

- **能编**：macOS 没有 Windows 那种强制文件锁，运行中的进程不挡 `dotnet build` 写输出目录。
- **⚠️ 两个必须分清的后果**：
  1. **新代码不会进当前进程** —— 运行中跑的是启动时载入的映像 ⇒ 编完**必须重启**才生效（这就是「我改了、屏上没变」的常见来源；本例因**是**自迭代，人重启后即正常）；
  2. **覆盖运行中的 dll 有风险** —— MSBuild 覆盖 = 就地重写那个文件，而它已被运行进程 mmap ⇒ 理论上可让运行中进程取到不一致映像/崩。
- **正解**：**编译/测试由 agent 做，重启由人做**（现状即如此）。别把「agent 自己重启自己」当成升级 —— 那是把一次崩溃换一次方便。
- **配套**：验证要落**二进制符号**（见 #75），别只看「0 警告 0 错误」（空转编译也报这句）。

## 77. 启动命令的「隐含前置」会随转发丢掉：相对路径只在某一个目录里成立（2026-09-17，作者实测）

- **症状**：`dotnet bin/Debug/net10.0/AgentRuntime.Tui.dll --config ../AgentRuntime.Cli/config.stage1.json` 报「找不到指定的命令或文件」；而 dll **存在**、也**含最新代码**（`SliceColumns` / `WhileThinkingAsync` 都在）。
- **根因**：命令里两个相对路径（dll 路径 + `--config ../…`）**都按 cwd 解析** ⇒ 只有 cwd = `src/AgentRuntime.Tui/` 时成立。文档里那行 `cd` 是**注释形式**，转发时被当成可选项丢掉，于是「命令没错、目录错了」。
- **辨别**：`which dotnet` 有、报的又是 .NET CLI 自己的话（不是 `command not found`）⇒ **dotnet 跑了，是路径没找到**，不是编译问题、不是 PATH 问题。
- **口径**：① 给启动器脚本（`run.sh`：先切到自身目录再执行，dll 缺失给可照做的提示）；② 文档把 cd 从注释升级成 **⚠️ 硬前置**，并写明「失败时会看到什么报错」；③ **给别人的命令要么 cwd 无关，要么把前置写在命令块里**。
- **通用**：与 #71（相对路径基准）同族 —— **凡路径，先问「按谁解析」**；凡交付命令，先问「换个人换台机器还成立吗」。

## 78. 工具结果只进事件流 ⇒ 免批动作在屏上等于「什么都没发生」（2026-09-17，[WB] 实测）

- **症状**：作者报告「只读的那些不需要批准，但他也不会自动汇报工具使用后的结果」——免批的 `read`/`list` 跑完，屏上什么也没有。
- **根因**：`ToolRunner` 把结果 `Append` 成 `ToolResult`/`ToolDenied` **事件**（给模型用），**没有任何面向人的打印**；而协议要求的「模型下一轮汇报」既可能漏，**没有自动接续时根本不会有下一轮**（与 #79 的「不停喊继续」是同一根病）。
- **口径**：显示层要**自己**画工具行（`→ read xxx · 12 行`）—— 只取事件**首条非空行**（工具自己写的自描述摘要）+「（还有 N 行）」，**绝不把正文倒进对话**。**免批 ≠ 不必看见**：免的是「批准」，不是「知情」。

## 79. 自动接续要「能自己停」：判据是模型不再点工具，不是数轮数（2026-09-17，对照 OpenClaw 设计）

- **参考实现**（OpenClaw `docs/concepts/agent-loop.md`）：runtime 自己把工具结果回喂、自己推进下一轮（**用户不用推**）；**终止由模型决定**（某一轮不再要点工具 ⇒ 循环结束）；预算是**经过时间**且**进度不重置**；**审批等待会暂停未用预算**；人可 `Stop` / `steer` / `interrupt`。
- **WB 原先的缺口**：`--auto-continue N` 只在 CLI、默认关，**TUI 完全没有** ⇒ 每轮之后必须人喊「继续」（作者原话：「任务没有持续感」）。
- **口径**：① 触发判据**只看流**（工具结果/被拒已落地，不看模型自述）；② **正常出口 = 模型自己停**；轮数/时间上限只**兜底**且到点留痕；③ 预算钟与审批闸门**共用**（进审批面暂停、出来恢复）；④ 续跑轮的**预览必须与实发同源**（否则右栏显示的是「带用户消息」那份，T1 破线）。
- **通用**：**「自动」的正解是「有明确的停判据」，不是「跑得久」** —— 说不出「它凭什么停」的自动，就是失控。

## 80. 范围授权的例外清单：写 → 执行链（2026-09-17，作者选「审批 A 案」）

- **背景**：把「一次点头覆盖同类族」做进去（目录级写 / 命令形状级执行）时，最容易打开的洞是**「写一个东西，然后让它被执行」**。
- **口径（永久例外，写了仍逐次问）**：`.git/`（git 自己会跑 hooks）· 构建文件（`*.csproj`/`*.props`/`*.targets`/`*.sln(x)`）· 脚本（`*.sh`/`*.py`/`*.ps1`/`*.plist` …）· `Makefile`/`package.json`/`Dockerfile`；**判不出也算例外**（fail-closed）。
- **层级纪律**：范围授权必须排在 **硬拒 / must-ask / 宽能力 / 可疑可执行** 之后 ⇒ 那几类**天生盖不住**（不是靠约定，是靠判定次序）。
- **通用**：**给"一次免问"扩容时，先把"什么永远不免问"写死并测出来**；否则省下的每一次点头，都会以「某个洞被打开」的形式付回来。

## 81. 「唯一真相源」被写了两处，而两处互指 ⇒ 副本静默落后一版（2026-09-20，协议 v9 落地时发现）

- **症状**：`ProtocolText.cs` 的注释写「照 `REPORT-PROTOCOL-ZONE.md` §三 逐字」，而 `REPORT-PROTOCOL-ZONE.md` §三 的标题写自己是「**唯一真相源**」——两处互指。实测：协议升到 **v8** 之后，报告 §三 里那份**逐字正文还停在 v7**（规则 2 的文字都不一样），**而且没有任何闸门因此变红**（预算闸门只看代码常量）。
- **根因**：副本的同步靠**人记得**。而「改协议 = 缓存整体归零」这条纪律让人**不敢顺手改**（怕被人当成「你偷偷升了协议」），于是副本越拖越旧。
- **辨别**：凡是文档里出现「**逐字**」「**唯一真相源**」字样的地方，问三个问题 —— ① 它是对**哪一份产物**的副本？② 谁是**产生**它的那一份？③ **什么命令**能一眼比出差异？（答不出第三问 ⇒ 这个副本迟早骗人。）
- **口径**：① 真相源只留**代码常量**（可执行闸门钉住它）；文档里的副本一律标注「副本 + 核对命令」（`--protocol-show`）；② 副本落后**不算错误但必须可见**（本文就是这条记录）；③ 同族纪律见 §十·46「闸门自证：同源」。
- **通用**：**「唯一」这个词只能有一个作者**。两个文件互相说对方是唯一真相源 = 两边都能晚一版，而谁都不会发现。

## 82. 会话内重建模块 → **审批闸门静默丢失**（2026-09-20，加 `/reset` 时发现）

- **症状**：`/ablate`（会话内摘挂模块）、`/resume`（分叉后重建）之后，工具面**再也不问人**：屏上一切正常，工具调用一律被拒（或按「非交互默认档」处理）。加 `/reset` 时才发现这条路径从一开始就是这样。
- **根因**：`RuntimeHost.RebuildModules()` 调 `BuildModules(config, override, vacuum)` 时**没带** `toolGate` / `toolLedger`（装配时它们只在 `Boot()` 那一处传过）⇒ 重建出来的 `ToolModule` 拿到 `null` 闸门，退回 `NonInteractiveApprovalGate`（「非交互一律拒」）。
- **辨别**：**凡是「重新装配一个已经装配过的东西」的路径，先问「哪些参数是装配期才有的、且不会被配置记住」** —— 闸门 / 账本 / 限流档都是这类「不进配置文件的关系型依赖」。
- **口径**：宿主**留住**装配期的闸门与账本（`RuntimeHost._toolGate` / `_toolLedger`），重建时**复用**；测试断言 **`Assert.Same`**（不是「行为差不多」——闸门身份就是安全边界身份）。
- **方向性**：这个 bug 的失败方向是 **fail-closed**（拒绝而不是放行），所以没造成越权；但**静默降级**本身已经足够坏 —— 作者会以为「工具面坏了」，而不是「闸门丢了」。
- **通用**：**重建 ≠ 重配**。重建时丢的往往是「不在配置里的那部分上下文」，而它们通常正是安全相关的。

## 83. 协议里写了「当运行时说……」，宿主就必须真的说（2026-09-20，20% 收尾提议）

- **背景**：v9 第 7 条写「当运行时告诉你上下文已达窗口 20% 且任务完成 ⇒ 提议收尾 + 重开」。问题是：**token 用量是 provider 的事实，模型看不见自己的**。
- **若照字面实现**（只在屏上给人看一行提议）：协议里那条对模型**永远不可能触发** —— 又是一次「协议承诺运行时不兑现」（v5 不写 `[SKILL]` 的理由）。
- **口径**：宿主把提议**同时**（① 屏上给人、② **进流成 `Hint` 事件给模型**）。`SessionEventKind.Hint` 因此诞生：它是「宿主对模型说的一句话」的**唯一通道**（与 `ToolResult` 同族：都是宿主产生的事实）。
- **通用**：**协议里任何以「当运行时告诉你……」开头的条款，都要立刻回答「用什么通道告诉、谁产生那条消息」。** 否则那不是契约，是许愿。

## 84. 「改过时间」不是「交过了」：判据用时间戳 ⇒ 假绿（2026-09-20，写收尾件检查时当场踩到）

- **症状**：第一版 `handoff` 判据写的是「文件名含今天的日期 **或** 文件改过时间 = 今天」。测试里放一个 `.keep` 空文件占目录，判据立刻把它算成「今天交了 1 件」——**一条假绿**。
- **根因**：时间戳是**副产物**（复制 / 格式化 / 编辑器保存都会动它），它不承载「这份交接件是为这次任务写的」这个语义。**命名约定才承载语义**（`17-白板求解口径-给001-2026-09-20.md`）。
- **口径**：主判据 = **命名约定**（名字里带今天的日期）；「今天动过但名字不带日期」的文件**另报一行**（它是线索，不是证据）。同族纪律见 §十·35「量法错家族」。
- **通用**：**凡是「做了没有」的判据，先问「这个信号是谁产生的、能不能被无关动作伪造」**。时间戳 / 存在性 / 字节大小都太容易被伪造。

## 85. 「没配」「看不清」「没交」是三件事（2026-09-20，收尾件检查的三态）

- **背景**：收尾件的每一项都要回答「交了没有」。最容易的写法是 `bool`，然后 `false` 同时表示：工作区没配、文件读不出、确实没交。
- **代价**：三者混成一个 `false` ⇒ 人会去追一个**不存在的缺失**（「为什么说我没交 handoff？」——因为根本没配工作区），追完后开始**不信任这个清单**，然后它就死了。
- **口径**：三态 —— `Ok`（已对齐）/ `Pending`（**要处理**）/ `Unconfigured`（**没配 ⇒ 不校验，明说**）+ `Unknown`（**看不清 ≠ 没问题**）。缺件**只报不拦**（拦下的唯一后果是人绕开清单）。
- **通用**：**「没做」与「没法知道」必须分开报**；合起来报的诊断面，最后一定会被当成噪音关掉。

## 86. 自称「只读」的工具其实会写：收尾判定改了账本（2026-09-20，跑 `--closeout` 时被抓到）

- **症状**：`knowledge-repo.py --closeout` 的用法说明写着「三态判定（**只读、不写、不交互**）」。实跑之后 `svn status` 冒出两个 `M`：
  `workspace/knowledge/.ledger.json` 与 `whitebox/workspace/knowledge/.ledger.json` —— 它往 `closeouts` 里追加了一条 `"action": "reported-only"` 审计记录（态 A / 态 C 各一条）。
- **根因**：**判定要留痕**在工具作者看来是「记录」，在调用方看来是「写」。两边对「只读」的定义不同：作者指「不改 md」，调用方以为「不动工作副本」。
- **辨别（可复算）**：**跑前跑后各看一次工作副本**（`svn status` / `git status`）；凡工具自称只读而工作副本变脏 ⇒ 定义不同，不是工具坏了。
- **口径**：① 审计条目是**准确记录**（确实发生了这次判定）⇒ 保留并随本轮提交，不要偷偷 revert；② **要真只读的判据，自己实现**（WB 侧的收尾件检查就是纯读：`CloseoutChecklist` 只读文件、算指纹，不写任何东西）；
  ③ 工具用法行里的「只读」要注明**范围**（「不改 md」≠「不改工作副本」）。
- **通用**：**「只读」是相对某个范围说的** —— 引用别人的「只读」声明时，先问它读/写的是**哪一层**。

## 87. 「默认落点」= 隐式全局状态：测试/旁路进程写到了真状态上（2026-09-20，会话指针刚上线时当场发生）

- **症状**：加了「会话指针」（收尾/重开跨进程续接）之后，`dotnet test` 里 **40 条测试变红**；更糟的是**真会话的事件流被写进了测试事件**
  （同一文件里出现两段 `seq`：`E001…E010` 是某测试的 `read`/工具行，接着又从 `E001` 开始）⇒ 再启动时 WB 拒写：
  `事件流文件被破坏：期望序号 11，实际 8（只允许只追加写入）`。
- **根因**：指针默认落在 `~/.agentruntime/session.json`（**未配置时也有默认值**），而测试台里建的配置**没写** `lifecycle.handover`
  ⇒ 测试 boot 时**读到了真指针**、于是把测试轮次追加到**真会话流**上。**默认值让「没配」与「配了」在类型上不可区分**。
- **辨别（一句话）**：凡新增「有默认落点的全局状态文件」，先问：**没配置时，谁会读到它？**（答不出 = 迟早有人读到不该读的）。
- **口径**：① 生产默认**不等于**「没配也用」——把「显式配置」这一位**单独记下来**（`HandoverExplicit`），没显式配就**完全不读不写**；
  ② 测试夹具必须显式给临时路径（顺带保住「测试不许碰家目录」）；③ 事故现场**保留**（`.corrupt-…`）而不是删掉。
- **通用**：与 #86 同族 —— **「谁在什么时候读写它」比「它默认是什么」重要得多**。

## 88. 帧渲染与 append 并发：`Collection was modified`（2026-09-20，作者收尾时真机 abort）

- **症状**：收尾过程中 `whitebox` 直接 abort：`InvalidOperationException: Collection was modified; enumeration operation may not execute.`
  栈：`FocusService.Weigh`（枚举 `stream.Events`）← `FocusModule.CurrentFocus`（**属性 getter**）← `SplitSession.SelfReport()` ← `Frame()` ← 渲染回调 ← `WhileThinkingAsync` ← `TurnContinuation`。
- **根因**：`SessionAppendStream.Events` 直接把**活列表**交出去；**续跑轮在 append** 的同时，**帧渲染**（思考态的每 200ms 重绘）在枚举它 ⇒ 并发修改。
  换句话说：**`get_` 属性的外表掩盖了「它会遍历一个正在被写的集合」**。
- **口径**：① 写入口**持锁**（`AppendPreservingTag`）；② **跨线程读一律走 `Snapshot()`**（持锁拷贝当拍表）；
  ③ 渲染路径（每拍都可能跑）**必须用快照**，不许碰活集合。
- **辨别（一句话）**：**双线程 + 一个可变的 IReadOnlyList = 迟早炸**；`IReadOnlyList` 只保证「你不改」，不保证「没别人改」。
- **通用**：与 #39（墙钟混进可 diff 帧）同族 —— **渲染路径的输入必须是「当拍不可变」的**。

## 89. 输入法候选窗把终端输入区行高顶起来（2026-09-20，作者真机发现）

- **症状**：中文输入法弹出候选清单时，TUI 输入区**行高自己变高**（候选串消失又缩回）—— 视觉抖动。
- **根因**：输入区行数按**显示宽度软换行**算（`Wrap(text, width).Count`），而输入法候选串会短暂进入输入缓冲 ⇒ 宽度超一屏 ⇒ 多一行。
- **口径**：**行高只由「显式换行」决定**（`1 + count('\n')`，上限 5）；单行长文本改回**行内水平滚动**（光标始终可见）。
  取舍写清：长文本不再自动折行（换来的是**输入法不再顶行高**，且行高与内容一一对应、可断言）。
- **通用**：**凡是「用外部输入的形状驱动布局」，先问「这个形状里有没有我控制不了的瞬态」**（输入法 / 粘贴 / 组合键都是）。



## 90. 跨库同名文件致误判（2026-09-20，助手）

- **现象**：同机存在两份 `knowledge/knowledge.md`（OC 侧 `~/.openclaw/workspace/knowledge` / WB 侧 `…/whitebox/workspace/knowledge`），两者曾共用同一份 v3 快照（指纹 `7e94f383`）。
- **误判**：核实「知识库版本是否前进」时，拿 **WB 侧活版指纹**与 **OC 侧账本** 对比 ⇒ 得出「09-20 记忆里那句话是错的」。
- **真相**：那句话说的是 **WB 侧**，**原文正确**；OC 侧活版另有自己的指纹（`cd619892`）。
- **通用**：凡核实「某文件 / 某对象」的状态，**第一步先锚定路径**；同名文件跨库比对 = 量法错（同 §十·31）。

## 91. `grep 关键字` != `路径依赖`（2026-09-20，助手）

- **现象**：`grep -i openclaw` 在 L3 正文命中 16 处，看似「WB 仍依赖 openclaw」。
- **真相**：全为**方法论内容**（灰箱/白箱对比、`openclaw sessions list --json` 命令示例），**非路径依赖**。真正读取只有 3 处（看 `expanduser`/`cp`/`--workspace 默认` 调用）。
- **通用**：判定「是否依赖 X」必须区分「内容提到 X」与「代码真读 X」——前者 grep 得到，后者要看读写调用。

## 92. 顶层文件的「路径指针」会静默漂移：改名了，指路人没改（2026-09-21，[WB] 收尾时当场卡住）

- **现象**：[WB] 侧自己的 `AGENTS.md` 指向 `METHODOLOGY.md` 与 `memory/INDEX.md`，而本端实际文件是 **`KNOWLEDGE.md`** 与
  **`MEMORYINDEX.md`**（改名发生在 09-14 前后）。于是 [WB] 侧的模型**按铁则**去读 `memory/INDEX.md` ⇒ 「文件不存在」⇒ 收尾卡在
  「该更新哪个文件」上，整轮空转（`~/.agentruntime/closeout-20260921-212148.md` 之后那次会话）。
- **根因**：`AGENTS.md` 是 [OC] 侧的**旧副本**，改名时只改了实际文件，没人回头改这份副本里的**指针**。
  它与 #81（唯一真相源被写了两处）是**同族**：副本静默落后一版，而「落后」没有任何闸门看得见。
- **通用**：**顶层文件里被点名的路径必须存在** —— 收尾自检应当扫一遍 `AGENTS.md` 里的 `\`*.md\`` 形状，缺失即报。
  更进一步：副本类文件（各端 AGENTS/SOUL/USER…）改名时，**同一提交**里改三处（#81）：生成器来源声明 · 版本账本 · 文档指针。

## 93. 判据数「有没有文件」⇒ 「应该有而没有」看不见（假绿）（2026-09-21，[WB] 连续两天踩）

- **现象**：收尾件检查输出「✅ 会话记忆（水位线之后的）：水位 2026-09-17；晚于水位的日记 **0 篇**」——
  看着是绿，实际是**当天日记根本没写**（09-20 与 09-21 两次都这样）。
- **根因**：`CloseoutChecklist.MemoryItem` 只数「**已存在**且晚于水位」的日记；文件不存在 ⇒ 计数 0 ⇒ 与「本来就没有待整理的」
  长得一模一样。**缺件与无件不可区分**。
- **通用**：判据要按「**应有的集合**」而不是「**已有的集合**」算 —— 有 session 的日子都该有一篇日记，缺哪几篇要说出来（缺 N 篇）。
  与 #84（判据用时间戳 ⇒ 假绿）、#85（没配 / 看不清 / 没交 是三件事）同族：**三态不够时，缺的那一态会把错的说成对的**。

## 94. 新聚合器只喂合成用例 ⇒ 「开新卡没收上一张」这种错测不出来（2026-09-21，TaskLifecycle L0）

- **现象**：`LifecycleAggregator` 第一版在「开新生命周期」的分支里 `open = new Builder(...)` 却**忘了先把上一张收进结果**
  ⇒ 160 条的长会话只报 **1** 个生命周期（前面两张整份丢掉）。15 条合成单测**全绿** —— 因为每条用例都只有一张卡。
- **根因**：合成夹具只覆盖了「单卡」形状；**跨卡的累积**没人测。
- **修法**：修那一行 + 补两条用例（连续请求 / 让位），并把**真实流回放**当成入场仪式：
  `--stream "$PWD/.stage1/stream-20260920-142735.jsonl" --lifecycle-show`（160 条 ⇒ 应当 **3** 个）。
- **通用**：**先拿真实数据跑一遍，再信单测**。新写的聚合/投影类组件，第一次验收必须是**真实产物**（与 §十·31/35「量法错」同族：
  测不出来先怀疑量法/夹具，别先怀疑被测物）。

## 95. 「固化」冻的是过期快照：卡在它还活着时被画了一次（2026-09-21，TaskLifecycle L2）

- **现象**：生命周期卡在「它还是最后一张」时被画成 `● 进行中`；之后用户又说了下一句 ⇒ 那张卡其实已经是 `未终局`，
  但**没人重画它**（已经固化）⇒ 冻结的那一份在说谎。演示帧里一眼可见（`#1 ● 进行中` 却是被顶掉的那张）。
- **修法**：**关账那一刻按真实状态重画一次再冻**（`SplitSession`：开新卡前先 `RefreshCard(withHint: false)`）。
- **通用**：任何「定格 / 固化」都**快照化当时的状态**，别沿用上一次渲染的缓存（与 #85「看不清/没配/没交是三件事」同族：
  状态的定义时刻错了，闸门再绿也是假的）。

## 96. 演示文档四块全漂：转录 / 六区表 / 两帧全落后于产物（2026-09-21，TaskLifecycle L2）

- **现象**：`verify-demo.sh` 一直是「绿」的 —— 直到 L2 改了左栏渲染才发现：**文档改由产物生成才是常态**，
  而文档里那四块 ```text 全是**手抄**的产物（转录 / `--stack dump` 表 / 96×30 帧 / 80×24 帧），
  上游一变它们就静默落后。首次跑新写的 `sync-demo-doc.py --check` ⇒ **4 块全不一致**（含六区表里的字节与指纹）。
- **修法**：新增 `src/AgentRuntime.Tui/sync-demo-doc.py`（按「节标题 + 该节第几个 ```text 块」定位，**只换围栏体**，
  `--write` / `--check`）+ 与既有 `verify-demo.sh` **成对跑**；散文里引用的数字（「那 23 行」）进 STALE 清单。
- **通用**：见 skill `generated-view-sync`。要点：**产物块一律由生成器写**；**生成器与独立复验器成对**；
  改了显示层就把「`demo.sh` → `sync-demo-doc.py --write` → `--check` → `verify-demo.sh`」当**最后两条命令**。

## 97. `{x:P0}` 在 `InvariantGlobalization=true` 下会插空格（2026-09-21，TaskLifecycle L1）

- **现象**：期望 `命中 90%`，实际 `命中 90 %`（一位空格之差，单测红）。
- **根因**：本仓 `Directory.Build.props` 设了 `InvariantGlobalization=true`，该档下百分比格式带空格。
- **修法**：百分比一律手写 —— `(rate * 100d).ToString("F0", CultureInfo.InvariantCulture) + "%"`。
- **通用**：**格式化的「小差别」要当断言写进用例**（本次就是用例先红才看见）；跨文化设置下的数字/日期/百分比
  不要依赖默认 pattern。

## 98. 批量编辑脚本把「类本体」当成员删掉（2026-09-22，WB 撤闸门 v13）

- **症状**：为清掉引用已删类型的成员，写了个「按大括号配对切成员」的脚本；结果 4 个文件（`SplitSession` + 3 个测试）的**类声明连带整个类体**被删 —— 因为过滤条件里 `public ` / `sealed class` 也算"成员起点"。
- **后果**：`error CS0246 找不到类型 SplitSession` / `CS0116 命名空间不能直接包含成员`。
- **恢复**：`svn revert` 那 4 个文件（**只回到 BASE**）→ 重新精确施加改动。BASE 版本**不含未提交的其它改动** ⇒ 必须靠上下文把改动重新贴回去（本次贴回 `SplitSession` 2 处 + `SecurityGatewayTests` 5 处）。
- **通则**：① 批量编辑**先 dry-run 打印"将删名单"**；② 匹配**只认方法/私有成员**，类声明显式排除；③ 跑完立刻 `dotnet build`（别攒）。

## 99. 改闸门语义 ⇒ 测试样本会从「不执行」变成「真执行」（2026-09-22，WB 撤闸门 v13）

- **症状**：撤掉审批闸门后，原本"被拒 ⇒ 没执行"的用例变成**真的执行**。`AutoContinueTests` 拿 `svn commit -F /tmp/x` 当样本，而测试 cwd 就在 SVN 工作副本里 ⇒ **会真提交**；`SecurityGatewayTests` / `ApprovalV12Tests` 有写 `~/.ssh/...` 的样本。
- **修法**：把危险样本一律换成**无害样本**（`svn status`）、把真实私钥路径换成 temp 里的**凭据形状**文件（`api_key`），并把"发布类命令不在测试里真跑"写成纪律。
- **通则**：**改安全语义前，先扫一遍"哪些样本会从不可执行变成可执行"**。

## 100. 状态块承载了不属于它的内容（2026-09-22，WB 撤闸门 v13）

- **症状**：右上「状态 · 本轮」块里画了「就地展开的正文」，且计入 `StatusRows` ⇒ 展开后状态块被顶大、**tokens 行被挤掉**；而 `SplitView.cs:171` 自述的口径本是「右列上 = 状态（固定行数）· 下 = 详细（展开正文 / 面板输出，可滚动）」。
- **修法**：状态块只留 状态 + 目录 + 合计/tokens；展开正文改在**详细区**渲染（目录每一行下面）。
- **通则**：**展示层的每一块都要能说出"它为什么存在"**；一处偏离设计自述，症状会以"看不懂的挤"出现。

## 101. 全展开之后 ↕ 被"滚详细内容"抢走（2026-09-22，WB 撤闸门 v13）

- **症状**：Tab 切到右栏后 ↕ 动不了模块选中项。根因：`Scroll()` 先判 `详细内容非空 || 就地展开集非空` ⇒ 全展开时该条件**恒真**，选中项永远轮不到。
- **修法**：右栏 ↕ = 移动选中项（`MoveSelection`）；详细内容改用 **PgUp/PgDn**。
- **通则**：**一个键只能有一个语义**；把两件事塞进同一个入口，迟早会有一边"永远轮不到"。

## 102. 批量删块时，区间里**还有别人的成员**（2026-09-22，[WB] 闸门族清理）

清 `IApprovalGate` 族时，用「从 A 标记到 B 标记整段删除」的脚本一次删掉了审批成员；
B 标记选的是下一个成员的开头，而 **`Redraw` 属性正好夹在这两者之间** ⇒ 它被一起删掉，编译报 5 处 `CS0103`。
- **判据**：删块前**打印将删区间**（首尾行 + 行数）并扫一眼里面有几个成员；「首尾都是成员开头」不等于「区间里只有它俩」。
- 与 #98 同族（**批量脚本的边界**）：只匹配方法/成员级、先 dry-run 名单、回退前留兜底。
- 修法：`svn revert` 回 BASE 会连带丢未提交改动 ⇒ 用「重施精确改动」补回（本次即刻补回，未留半改状态）。

## 103. 终局块的「行内口径」吞掉块式正文 ⇒ 人看到空问题位（2026-09-22，[WB] 作者真机报）

- **症状**：模型按 `[TAIL]` / `[DRAFT]` **同一款块式写法**写 `[NEED-USER]`（块头独占一行、问题在**下面几行**），而 `TerminalReport.TryMatch` 只读**同一行** ⇒ `Detail` 为空。于是屏上出现
  `[终局] 需要人[NEED-USER]（未写正文）` + `[终局] 它在等你（要决定 / 要信息）—— 回一句即可。`，
  生命周期卡上是 `待你决定：` + 一个**空项目符号**。**人被告知要回话，问题一个字都没有**（作者的原话：「问题都没摆出来，我怎么回复？」）。
- **判据**：宿主说「该你说话了」时，**必须能同时指出说什么** —— 只有「该你了」是空的礼貌。
- **修法**：正文口径与 `[TAIL]` / `[DRAFT]` 统一（行内写法优先，否则取块头之后、**下一个块头之前**那几行）；正文真为空时**不画空问题位**，改成一句明确告警 + 指出去哪看原文（`/trace`）。
- **通则**：**同一个回复里的块，解析口径必须一致**；「只有它行内」这种特殊口径，迟早被模型的自然写法打穿。

## 104. 撤闸门只改了行为、没改**文案** ⇒ 屏上还在承诺「问人 / 审批面」（2026-09-22，[WB]）

- **症状**：v13 撤掉硬闸门后，屏上仍有五处**骗人口径**：`--help` 的「审批面 ←→ 选不同意/同意 … Esc 全部拒绝」、`/grants` 的「问人 N 次 · 直接拒绝 N 次」（**没人被问、也没人被拒**）、`/strict` 的「逐条问人」、撤销授权的「会重新问你」、未登记工具拒绝文案里的「归对外类动作（must-ask），没有通道时不执行」。人读到的是「需要人类回复」，而世上没有任何询问。
- **判据**：**骗人口径** = 屏上文案与运行时行为不一致（本项目既有词）。撤语义时必须**同时**扫文案。
- **修法**：五处一次性对齐 v13（免问 / 留档 / 「原判该问人·该拒」）；`--help` 那行改成「v13 起没有审批面：本该问人的动作只留档、照样跑，屏上不再出现 y/N」；拒绝文案去掉 must-ask 并明说「这一条不是要人点头，只是不合协议」。
- **通则**：**改语义的提交里，文案与测试期望值都在清单上** —— 本轮正是被 `ApprovalV12Tests` 里一条断言「逐条问人」的用例挡下来，才发现漏改一处。

- **同族再现（2026-09-22，[WB] 流指针）**：`config.stage1.json` 的 `stream.path` 一直停在 **09-20** 那一卷，而实际挂的是 09-21 卷（启动时按**会话指针** `~/.agentruntime/session.json` 接上 ⇒ 配置那一格**静默过期**）。判据：指针（真相源）与配置（默认口径）必须一致，`/session --check` 会报「指针 ↔ 当前流不一致」——
  **但它只在你主动敲它时才看得见**。已对齐，并把不变式写进 `_stream_comment`：每次 `closeout→reset→start` 都新建一卷 ⇒ **重开后把该字段同步成新卷**。


## 105. 块形 token 的「有主」判据：grep 字面量查不出活块（2026-09-22，[WB]）

- **症状**：复核「R1 协议区还能不能再精简」时，`[FOCUSREPORT]` 被判为**死词**（全库只出现 1 次 = 协议正文自己），
  差点删掉它「零知识损失」。实际它**是活块** —— 运行时块头，由
  `SessionEvent.Label => $"[{Kind.ToString().ToUpperInvariant()}]"` 从**枚举名派生** ⇒ 源码里**没有这个字面量**。
- **判据**：块形 token 分三类，各有其主：① **模型发的块**（`ReportBlockHeaders`，解析边界）；
  ② **运行时发的块**（事件标签，由枚举派生）；③ **元记号**（`[PROTOCOL]` / `[KIND]`，非块）。
  「两张表管两件事」：能当解析边界的必须是①，②**不进**那张表（否则把注入事件当用户块吞掉）。
- **修法**：做成闸门 `Gate_Protocol_EveryBlockTokenHasAnOwner` —— 正例（现正文零孤儿）+ 负例（喂 `[FOOBAR]` 必须被抓）
  + 反向自检（白名单须含 `[FOCUSREPORT]`、正文须仍提它；谁删那澄清句 ⇒ 变红）。
- **通则**：**grep 关键字 != 路径依赖**（#91）的第三面 —— 名字若由**枚举 / 拼接**派生，必须按派生规则查，
  或直接问运行时要一份；**「grep 不到」永远不是删除的依据**。

## 106. 「卡上只给首行」= 模型的答复正文在屏上根本不存在（2026-09-22，[WB] 作者真机报）

- **症状一（正文不可见）**：作者问「A 案具体是什么」，模型**答了**（E031 里是一张逐文件的 6 行表：改哪个文件、改什么），
  但屏上「什么都没有」—— 因为 L2 把**正文移出左栏**（`SplitSession.LandTurn` 只把正文存进 `_lastProse`），
  卡上只有 `结果：{FirstLine(prose)}`（**一行**），全文只在 `/result` 里。人于是判定「它根本没给方案」，追问又得不到 → 连着问了两轮。
  ⇒ **修**：卡上 `结果：` 改为**末轮正文**（与了结态的 `结果：` 同口径），只做**行数**预算（>12 行给「还有 N 行 ⇒ /result」指针），
  **行内文字不截断**（§十·34）。一张卡原地更新 ⇒ 仍然「一次请求 = 2 条条目」，但答案在屏上。
- **症状二（同一段文字两遍）**：得解后 `[终局] 得解[DONE]：<正文>`（info 行）与卡上 `结果：<正文>` 是同一段文字
  ⇒ 屏上出现两次。**修**：split 模式掐掉那一条 info 行（判据同源：对同一份 response 再 `Parse` 一次取 `Describe()`），
  辅助行（「它在等你」/「冲突说明」）照旧；纯文本 / CLI 路径不走这里，照旧由 info 行承载（那边没有卡）。
- **症状三（行内块头泄漏）**：模型把 `[FOCUS] E101 …` 接在 `[DONE]` **同一行**末尾 ⇒ 终局正文把自报块带了进来，
  卡上 `结果：` 漏出 `[FOCUS] E101 E105 …`。**修**：行内正文**到下一个块头为止**（与分行块界同一份块头清单；必须**整词**匹配，
  `[FOCUSX]` 这种野标签不动）。
- **通则**：**「给谁看」这件事要落到具体的显示位** —— 把内容的载体从「消息流」搬到「卡」时，
  必须同时问「人在这张卡上读得到读不到」；否则「按设计不铺」会变成「按事实看不见」。
  同一个内容有两处渲染面时，**挑一处承载、另一处留指针**，别让两处都印全文。

## 107. 「取消」没人接 ⇒ 整个 TUI 被 abort（退出码 -6）（2026-09-22，[WB] 作者真机报）

- **症状**：作者说一句「OK 我们做个收尾吧」，TUI **自己退出了** —— 流里只有那条 `UserInput`，之后什么都没有（连一次状态写入都没有）。
- **复现（可复算）**：把他那卷真流原样搬进隔离沙盒 → 起一个**只 accept、不回复**的端点让请求真挂在飞行中 → 思考中按 Enter（插话）/ Ctrl-C 一下
  ⇒ 进程以 **-6（SIGABRT）** 退出，尾巴上是一条
  `Unhandled exception. TaskCanceledException … ObjectDisposedException: 'NetworkStream'`，
  栈一路从 `OpenAICompatibleClient.CompleteAsync` → `Engine.RunAsync` → `RuntimeHost.RunTurnAsync` → `PanelRouter.RunTurnAsync`
  → `SplitSession.WhileThinkingAsync` → `RunTurnOnceAsync` → `SubmitAsync` → `HandleKeyAsync` → `RunInteractiveAsync` → `Program.Main`。
- **根因**：`WhileThinkingAsync` 直接 `await work` —— **「这一轮被取消」没有任何人接**。而 `CtrlCQuit` 的契约恰恰写着
  「一下 = 中断当前轮，**进程不死、会话继续**」⇒ **契约与实现相反**（这条契约从未被测试钉住）。
- **修法**：`RunTurnOnceAsync` 用 `try / catch (OperationCanceledException) when (turn.IsCancellationRequested)` 接住 ⇒ 画一行
  「⏹ 本轮已中断（会话继续）」+ 刷新卡；`finally` 收表 / 清 `_turnCts` / 重绘；TUI `Program` 加一层兜底 catch（OCE ⇒ 130）。
  测试：**会挂住的假模型** + 中途取消 ⇒ 断言不抛且屏上有「本轮已中断」。
- **通则**：**每一个「可被取消」的 await 都要有人接** —— 中断是**正常出口**，不是异常。
  「契约里写了进程不死」就要有测试钉住它；UI 的取消路径尤其容易漏，因为成功路径总是先跑通。

## 108. 结论别只落收尾报告：运行时状态区不入备份，读它还触发留档（2026-09-22，[WB]）

- **来源（本轮真机）**：作者问「3 个既有红灯是什么」。准确名目在 `handoff/19` 与 `memory/2026-09-22.md`（工作区，可定位），
  但同一批结论**也**被写进了 `~/.agentruntime/closeout-*.md` —— 而那份**在受保护范围内**。
- **实测行为**：`read ~/.agentruntime/closeout-20260922-015054.md` 与 `ls ~/.agentruntime/`
  ⇒ 被判「受保护路径（私钥 / 运行时状态 / 判定者自身）」，**记一条留档事件后放行**（v13 语义：不再问人）。
  ⇒ 即：**读得到，但每次读都在账上留痕**，且它不属于工作区。
- **真正的风险**：`~/.agentruntime/` 是**运行时状态区** —— ① **不进 SVN 双向补录范围**（不在工作区）；
  ② 换机 / 换端 / 回滚后**不保证存在**。若有结论**只**写在这里（收尾件逐项结论、红灯名目、水位细节），
  它就会**静默消失**，而下一次会话无从知道它曾经存在（与 #93 假绿同族：应该有而没有，看不见）。
- **通则**：**收尾报告 = 过程日志；结论一律回落到工作区**（`memory/` 当日文件 · `handoff/` · 项目 `PITFALLS.md`），
  报告里只留指针。判据一句话：**「换一台机器还读得到吗？」读不到 ⇒ 它没资格当结论的落点。**
  与 #92（指针漂移）/#93（假绿）同族：三者都是「该在的东西没在该在的地方」。

## 109. 「累计用量」把每轮的整份 prompt 相加 ⇒ 同一份上下文重复计数（2026-09-22，[WB] 作者真机一眼看穿）

- **来源（作者真机）**：单栏顶框「会话」行的 `本 task +N tok` 在一场收尾后显示 **577k**；作者当场判「明显偏大，算法不对」。
- **事实（可复算）**：那一场收尾的流（`.stage1/stream-20260922-023957.jsonl`）里有 **32 条 `AgentOutput`** = **32 次模型调用**；
  而 `本 task` 取的是 `Σ(每轮 prompt + 输出)`、`缓存命中` 取的是 `Σ(每轮 cached)` ——
  **同一份上下文被按轮重复计入**，32 轮自然累出几十万。
- **算术没错，口径错了**：每轮的 `prompt_tokens` 都是**整份 prompt**（含全部缓存前缀），它回答的是「**这一轮**读了多少」，
  不是「**这件事**新增了多少」。把「每轮读数」直接相加 ⇒ 得到的是一个**没有对应事实**的数。
- **修法（v16）**：两个数各给唯一含义 —— `本 task +N` = **真正新增**（`未命中输入 + 输出`）；
  缓存改用**命中率**（`cached / prompt`）。绝对值天然随轮数膨胀，拿它当「本 task 用量」就是重复计数。
- **闸门**：顶框口径有 `口径_状态块说本生命周期_Δ列说Δ周期`；演示帧（`.demo/frame-*.txt` → `docs/TUI-DEMO-SAMPLE.md`）
  走成对流程重生产；设计文档 `docs/DESIGN-TUI-SINGLE-COLUMN.md` §8.1 同步写明新口径。
- **通则（与 §十·71 同族）**：**屏上每一个数都要能说清「它对应哪一件事」**。
  跨轮累计量（token / 字节 / 事件数）先问一句：**这个量本身会不会随轮数重复？**
  会 ⇒ 要么改成**增量**、要么改成**比率**；别把「每轮读数之和」当「本次任务的消耗」。

## 110. 拒绝话术「一次只暴露一个约束」⇒ 每轮买一个约束（2026-09-22，[WB] 作者真机量出来的）

- **来源（作者真机）**：一场**简单提问**跑了 **9 轮** / **16.343k new token**。逐条读流（`.stage1/stream-20260922-030751.jsonl`）：
  其中 **3 轮是纯协议对抗** —— E002 猜 `bash`（未登记）被拒 ⇒ E004 改 `exec` 但键写 `cmd` 被拒 ⇒ E006 改 `command` 却把 `risk` 塞进 JSON 被拒。
- **根因不是模型笨，是话说得省**：三条拒绝各只暴露**一个**约束（名字 / 键 / 形状），于是模型**每轮买一个约束**。
  第 1 条已说「最小集：read / list / write / edit / exec」，却**没说每个工具收哪些键**，也**没说 `risk` 写在 JSON 外面**。
- **修法**：`ToolRunner.Deny`（协议/语法类拒绝的**唯一出口**）一律附上**完整工具面**：名字 + **每个工具的键**
  （由 `ITool.ArgumentNames` 暴露；顺序按 `ToolNames.All` 定死 ⇒ 字节稳定可 diff）+ 完整形状
  （`[TOOL] 「名字」 {json} risk: 「none|…」`，**risk 在 JSON 外、同一行**，一次回复只发一个）。**fail-closed 一字未改**。
- **闸门**：`ToolFaceTests.拒绝时_工具面一次说全_不用一轮猜一个约束` —— 三种错法各打一次，
  每条拒绝都必须带 `【工具面】可用：` + `read{path,maxLines,offset,limit}` + `exec{command,timeoutSeconds}` + `risk 写在 JSON **外面**`。
- **通则**：**一次拒绝要把整张契约说完**。纠错信息的判据是「**读完这一条，下一轮能不能直接对**」——
  只报「这里错了」而不同时报出**全部合法取值**，等于把一次失败拆成几次失败（每次都要再烧一轮上下文）。

## 111. 「一次回复只准点一个动作」⇒ 每轮买一个动作（2026-09-22，[WB] 作者真机量出来的）

- **来源**：同 #110 那一场 —— 简单提问 **9 轮 / 16.343k new token**，其中 **4 轮是「读一点、看一点、再读一点」**
  （read 70 行 / exec / read 40 行 / read 45 行）。
- **根因**：协议第 5 条写死 `one call per reply` ⇒ 一次回复只能点一个动作，取证只能一轮一轮地挤；
  而**每轮都要重发整份上下文**（缓存前缀 + 流尾部）⇒ **轮数是成本的乘数**，不是常数。
- **修法（v14）**：一次回复**最多 4 次** `[TOOL]`，**按发出顺序**逐个执行、逐个回结果事件（一一对应不变）。
  超过 4 次 ⇒ 拒绝（不取前几个）；**任一块不合语法 ⇒ 整篇拒**（不跑一半）。上限仍设 4：
  无限点动作会让结果体量失控，也让人看不懂这一步在干什么。
- **同批**：第 2 条把 `never in bulk` 的**边界**写清 —— 那条管 **knowledge**；**读源码回答问题是「一次读够」**，
  不许用小窗口一轮一轮挤（实测模型把「不许 bulk」套到了读源码上）。
- **闸门**：`ToolReportTests`（多次全取 / 超限拒 / 一块坏整篇拒）· `ProtocolZoneTests`（版本 v14 · 实测 995 token ⇒ 预算 ≤12 行 / ≤1,100 token）·
  演示双闸门 + PTY e2e。
- **通则**：**凡是「一轮只准做一件事」的纪律，都要先问一句「这一件事值一轮的钱吗」**。
  一轮 ≈ 一次整份上下文重发；只有把「安全 / 确定性」的收益说清楚，才配得上这个价。

## 112. 「把读的边界放宽」⇒ 工具结果体量 10×（2026-09-22，[WB] 作者真机 + 我的回改）

- **来源**：协议 v14 我改了第 2 条，把 `never in bulk (no whole doc or log)` 的边界「写清楚」成 `read enough in ONE call`。
  同一道简单提问的对照：**9 轮 / 16.3k → 12 轮 / 37.1k**（工具结果 **13.8 KB → 139.9 KB**，占新 token 的 ~94%）。
- **根因（三条，缺一不可）**：① 旧措辞虽然在**误伤**读源码，但它**事实上在压着模型读小窗口**（改前它用 `limit 40/45/70`）；
  换成「一次读够」⇒ 它改成**整份文件倒**（单次顶到 8 KB 上限）；② v14 放开「一次最多 4 次调用」⇒ **4 × 8 KB = 32 KB/轮**；
  ③ 路径基准不明（**17 次失败调用**）⇒ 轮数反而涨。
- **修法（v15）**：① 措辞**收正**（要齐「你需要的窗口」，**绝不整份倒**）；② 单条结果上限 **8,000 → 2,000 字符**，**每条截断自带指针**；
  ③ `read` **行窗与字符窗对齐**（装到字符上限为止 ⇒ 头部行号 = 实际可见量）；④ `read`/`list` 失败话术**说清错在哪**并带上**基准目录**。
- **通则**：**改「放开 / 收窄」类口径时，两个方向都要量** —— 你要修的那一面（模型够不着）修好了，
  另一面（放进了多少字节）有没有炸。一句话：**口径只管方向，不管体量；体量得另立闸门。**

## 113. 「形状示范」会教坏模型：键清单被照抄成非法 JSON（2026-09-22，[WB] 作者真机量出来的）

- **来源（作者真机）**：协议 v16 第 5 条补了「工具名 + 各工具的键」，写法是紧凑的 `exec{command,timeoutSeconds}`。
  模型**照抄这个形状**，写出 `[TOOL] exec {command: "…", timeoutSeconds: 60}` —— **JSON 的键没有引号**
  ⇒ 不是合法 JSON ⇒ `ToolArgs.Parse` 拒（fail-closed）。实测：一场 **11 轮**问答里 **3 轮纯浪费**
  （E003/E004/E005 三条 `ToolDenied`，第 4 轮才自己改对）。
- **根因**：那张「键清单」**长得像 JSON**，而它其实只是**名字的列表**。协议同时给了 `"name {json args}"`（模糊）
  和键清单（像 JSON 但没引号）⇒ 模型把两者**合并**，得到一个**没引号的 JSON**。
  **给模型看的示范里，形状的权重远大于散文描述 —— 你写什么形状，它就抄什么形状。**
- **修法（v18）**：第 5 条**把形状说死**并给**带引号的完整示例**：
  `shape: [TOOL] name {"key":"value"} risk: none` + `[TOOL] read {"path":"src/Program.cs","maxLines":80} risk: none`；
  键清单**降级为附录**，明确标注 `key names per tool` = **名字、不是 JSON**（**键清单不能替代形状**）。
  同步（非协议区、0 缓存代价）：拒绝话术 `ToolRunner.ToolFaceContract` 补带引号示例，并把过时的
  「一次回复只发一个」改成「最多 4 个」（v14 起上限 = 4 —— 旧文案在**教错上限**）。
- **闸门**：`ProtocolZoneTests.Gate_协议正文里的工具面_与代码同一份渲染` 增断言（含 `shape: [TOOL] name {"key":"value"}` /
  `keys are quoted JSON` / `key names per tool:`）；`ToolFaceTests` 断言每条拒绝含「JSON 的键要带引号」+「一次回复最多 4 个」。
- **通则**：**省略引号 / 括号 / 分隔符的「紧凑写法」是最强的示范** —— 会被读成「原来可以这样写」。
  要省字节，就把缩写**明确标成名字或占位符**，并把**能照抄的完整形状**单独给一份；
  且那份必须真的能被运行时的解析器接受（**形状与解析器同源**，否则示范本身就在教错）。

## 114. 单条结果上限：**两个方向都比 2,000 差**，页数不由上限决定（2026-09-22，[WB] 后台 CLI 五组 × 3 跑）

- **来源**：作者问「读的 **2,000 字符上限改成 1,000** 会不会好一点？」。做法：同题（「没输 `/start` 会怎样」）
  ·同配置（真模型 `deepseek-v4-flash` + `frozen-private` A 档 + 全模块）·隔离状态·**各 3 跑**，另加两个变体。
  原始证据 `benchmark/runs/20260922-1619-cap-ab-token-efficiency/`（含 `summary.md` + 每组流与 `-v` 诊断 + 驱动脚本）。
- **实测（新增 token = `Σ(prompt−cached+completion)`，不是字节折算）**：

  | 组 | 轮数中位 | 新增 token 中位 | 结果条数 | 结果 KB | `SessionLifecycle.cs` 被读 |
  |---|---|---|---|---|---|
  | **2,000（基线）** | **6** | **13,697** | 12 | 21 | 3/4/4 |
  | 1,000 | 11 | 20,563 | 27 | 25 | 6/5/9 |
  | 4,000 | 7 | 17,330 | 16 | 30 | 3/3/3 |

- **两条机制（都反直觉）**：
  ① **调小**（1,000）：窗口减半 ⇒ **调用翻倍**（12→27 条）。截断率只从 21% 升到 29%（模型确实学会了收窄请求），
  但要覆盖**同样的材料**就得多读几次；而且**结果总字节不降**（21→25 KB）—— 从 `offset` 续读自带指针开销与边缘重复。
  ② **调大**（4,000）：窗口变干净（固定 3 段不重叠），但体量 **+43%**、token **+27%**。
  ⇒ **页数主要由模型的检索策略决定，不由窗口大小决定**（3~4 段 → 3 段，只降 19%）。
- **判据（下次照抄）**：一轮 ≈ **1.0~1.8k** 新 token（实测 13.7k/6 轮、12.6k/7 轮、7.0k/7 轮），
  而 2,000→1,000 每条只省 ≈250 token ⇒ **省 4~7 条才够买一轮**。**上限的收益是「省字节」，代价是「多买一轮」——
  两者不同量级，不能只算字节。**
- **通则**：任何「上限 / 阈值」类改动，**先问它会不会多买一轮**；会 ⇒ 必须实测，不能凭字节估算。

## 115. 给模型列「未读段」= 递一张待办清单 ⇒ 它去读满（2026-09-22，[WB] 同上五组实验）

- **来源（我做坏的一次）**：为了治「同一文件被 read 3~9 次、窗口大量重合」，我在 `read` 的结果里附了
  `（已读图：280-469；本次与已读重合 138 行；**未读段：1-279 · 501-582**）`，指望它**跳到未读段**。
  实测：新增 token 中位 **13,697 → 21,477（+57%）**，轮数 6→8，结果 KB 21→31。
- **根因**：**「未读段」是一张待办清单** —— 亮出来它就去读满；我本意是省页，实际是**加活**。
  收紧成「只在真的重复读同一片时出一句『这段已有结果，别重复读』」（**不列未读段**）后：**仍然涨**（中位 22,620）。
  ⇒ **「重叠翻页」本身不是浪费，是「放大镜式细化」**：先粗窗扫一片（例 `offset=280,limit=190`），
  再对里面关键的一小段换细窗（`332,120` / `382,90`）。**重合是细化的机制；告诉它"你读过了"等于打断它放大。**
- **修法**：**全部回退**（`rm` 新文件 + `svn revert` 三处；`dotnet test` 580/580 复验、`svn status` 只剩 `CHANGELOG.md`）
  —— **没验证过的东西不留代码**。
- **通则**：**别把"还没做的事"列给模型看**（那是待办清单，它会去做）；要引导就**只就它当前的动作说一句**。
  以及：**"看起来是重复劳动"要先证伪再修** —— 这次看着最像浪费的那一项，恰恰是有效机制。

## 116. 改「工具结果的内容」会改模型行为，方向常常反直觉 —— 必须 A/B（2026-09-22，[WB] 本轮四次改动）

- **来源**：本轮为「减少翻页」连做四次改动，**四次全部更差或持平**：
  上限 →1,000（+50%）、上限 →4,000（+27%）、结果附「已读图 + 未读段」（+57%）、只附「重合警报」（+65%）。
- **根因**：工具结果**不是被动输出，是控制面** —— 它同时是模型的**唯一观测**与**行为触发器**。
  改它的措辞 / 附注 = 改模型的行为分布，而这个分布**不服从「字数越少越好」的直觉**
  （#112 的「体量」、#115 的「待办暗示」都是这个族）。
- **修法 / 纪律**：凡触及**结果正文、上限、附注、拒绝话术**的改动 ⇒ **先跑同题 3 跑 A/B**
  （`drive.py` + `metrics.py`，判据 = 轮数 + 实测新增 token），**赢了才上、输了就回退**。
  **不许凭「看起来合理」直接改生产行为。**
- **通则**：**协议区 / 工具面这类「给模型看的东西」，是控制面而不是文档** —— 改动成本按
  「要不要重新养行为」算，不按「改了几行」算。

## 117. 加了一个「能力」（协议 v19 按符号取整段）—— 但**模型不用它** ⇒ 无可测收益（2026-09-22，[WB] 作者令「动协议区」）

- **背景**：前四刀（上限 1,000 / 上限 4,000 / 已读图+未读段 / 只报重合 / 一次要齐）全部更差
  （#114~#116）后，作者令「**动协议区，这次动完基本不用再动**」⇒ 做 **v19**：第 5 条把 <c>read</c> 的键补上
  **`symbol`**（`read{path,symbol,maxLines,offset,limit}`）并教一句「要看清一个东西的定义，就**按名字一次取整段**，
  别用小窗口一页一页翻大文件」；实现 = `ReadTool` 的词边界匹配 + 花括号配平（启发性，配不平则截到文件末并明说；
  `symbol` 与 `offset/limit` 互斥）。实测 **12 行 / 5,073 字符 / 1269 token**（预算 ≤13 行 / ≤1,300，未超）。
- **实测（n=6 真机同题）**：warm 5 跑的新增 token **13,276 / 10,656 / 10,731 / 25,034 / 22,225**（中位 **13,276**）
  vs v18 基线 13,697 / 12,916 / 15,945（中位 **13,697**）⇒ **基本持平**（差 <3%，在方差内）。
  轮数 5~10、结果 KB 17.8~40.2，与基线同量级。
- **关键事实（机制根本没触发）**：6 跑里 **`symbol` 只被调用了 2 次**（run2/run3 各 1 次，其余 4 跑 0 次）。
  ⇒ **收益无从谈起** —— 一个「能力」写进协议，不等于模型会用；而按 #115 的教训，**再加话去推它又会反噬**。
- **一次性成本（改协议的税）**：改协议 = 前缀冷启 ⇒ **首跑多付 ≈ 50k token**
  （实测 run1 第一轮 `cached=384 / prompt=49,773`，后续跑 49,536 命中）。而且**再改一次又要付一遍**。
- **决定**：**保留 v19**（无回归 + 真能力 + 在预算内；再改回去还得再付一次冷启），
  但**能效这条轴判为到顶**：五次改动 + 一个能力，**没有一次带来可测收益**；
  同题成本稳定在 **≈ 1.3 万新增 token / 5~7 轮**。**再往下要动的是模型本身或题目规模，不是话术。**
- **证据**：`benchmark/runs/20260922-1619-cap-ab-token-efficiency/v19-symbol/`（6 跑的流 + `-v` 诊断）。
- **通则**：**「加了能力」与「能力被用上」是两件事** —— 判据必须是**调用次数**，不是协议里有没有这句话。
  另：**改协议 = 一次性冷启税**，所以「回退一个无收益的协议改动」本身也要花钱 ⇒ 无回归的改动**留着比改回去便宜**。

---

## 118. 模型写「**假终局块**」（把 `[NO-SOLUTION]` 当「本轮没做完」用）⇒ 任务当场被判无解（2026-09-22，[WB] 作者真机报）

**现场**：作者第一个问题（Word→PDF 方案）**中途换问题**，进入第二个 TASK 生命周期 —— 结果卡上显示 **✗ 无解**。
`--lifecycle-show --stream .stage1/stream-20260922-164509.jsonl` 回放：`#2 ✗ 无解 · 结果：本轮不发终端块 —— 取证未完，继续。`
—— 那行「结果」**就是模型自己的字**：它想表达「本轮不终局」，却借了 `[NO-SOLUTION]` 这个块头。

**判定链（运行时没错，输入错了）**：`TerminalReport.Parse` 认行首整词块头 ⇒ `NoSolution` ⇒ `IsSettled` ⇒
「终局即停」（`TurnContinuation`）⇒ 同轮 **8 个工具**的结果回来后再无下一轮（模型没机会改口）。

**根因（协议层，不是知识层）**：协议第 6 条只**正面**列三种终局块，**没有负向口径**（没结束就不发块 ⇒ 不发即继续）；
而第 5 条又要求「回复末尾要有块」⇒ 两句合起来把模型逼成「每轮必须用一个终局块收尾」，于是它发明了假块。

**修法（协议 v20）**：第 6 条补三条 —— ① 没结束不发终局块；② `[NO-SOLUTION]` 只表示**已证此路不通**；③ 同轮还在点工具不算终局。

**通则**：① **任何「终局 / 完成」判定都要有负向口径**（只给正面枚举，模型会从里面硬挑一个）；
② **令牌的字面意思会被当真**（`NO-SOLUTION` 被读成「没有终局的块」）—— 命名与教学都要写清「**什么时候不用它**」；
③ 这类判据属**内核契约（协议区）**，**不靠用户知识库兜底**（作者 2026-09-22 17:0x 定）。

---

## 121. 输入判定看「首字符」而不是「名字」⇒ 粘一个绝对路径被当成斜杠命令吃掉（2026-09-22，[WB] 作者真机报）

**现场**：作者把 `<repo>/whitebox-workspace` 粘进 TUI 输入区
⇒ 整条被当命令（未登记），文本没能送出去。

**根因**：`IsCommand(line) => line.TrimStart().StartsWith('/')` —— **路径与命令共用同一个首字符**，
判定维度选错了：命令的身份在**名字**里，不在第一个字符里。修法见 CHANGELOG **0.10.11-dev**
（`PanelRouter.Commands` 名单 + `//` 转义 + 「名单 ↔ 帮助文案」双向闸门）。

**通则**：① **一行输入的身份由「首个词」决定，而不是由「第一个字符」决定** —— 凡把「前缀字符」当类型的判定，
迟早吞掉合法的自然输入（本坑与「拿时间戳当提交状态」#84 同族：**选错了裁判**）；
② 白名单必须与帮助文案**双向**对齐（名单有而帮助无 ⇒ 用户不知道；帮助有而名单无 ⇒ 那条命令永远跑不起来且**静默**）；
③ 判定规则要留**转义**口（`//`），否则「想发一条恰好以命令名开头的文本」没有正路。

---

## 122. 形状容差不足 ⇒ 「求解口径」长期**静默丢**（模型 44% 把 `solve:` 写成列表项）（2026-09-22，[WB]）

**现场**：做「卡的五段叙事」时，②「意图 / 打算」在**三张真卡上全空**。查流发现：`[TAIL]` 头两行有 **39/88（44%）**
写成 `- solve: …` / `- step: …`（列表项），而 `SolveHeader.Parse` 只认**行首裸标签** ⇒ 解析返回空，
白板「求解」面板与卡上两段**一起静默丢**（不报错、不告警，没人会发现）。

**通法**：① 解析「人写的口径」要把**常见等价写法**当同一件事（列表项 / 编号项 / 裸标签）——判据不是「我规定的形状对不对」，
而是「**实际流里的形状覆盖了多少**」：**先量分布再定容差**（实测 44%，不是边角）；
② 与坑 **#103/#104**（块式正文、行内块头）同族：**容差不足 = 证据没了**，而且**不报错**（最贵的一类静默失败）；
③ 修容差只动**读取** —— 正文照旧逐字进 prompt（不改写模型写的东西）。

---

## 126. 「中断」与「接续」在流里都没有痕迹 ⇒ 模型只能把接续句当全新问题去猜（2026-09-22，[WB] 作者真机报）

**现场**：作者按 Ctrl-C 暂停，紧接着发出的那句是**被打断那一轮的接续**；新会话却把它当全新请求——
`.stage1/stream-20260922-231302.jsonl` E001 `还是使用 | 这个符号分割` 之后，它**满仓库找这句话的意思**
（grep `分隔/分割`、`竖线/管道`、`Separator/Delimiter`、读 `SplitView.cs`、`grep 全角/中点/竖线`），
还把猜出来的意思写进白板：`solve: 定位「还是使用 | 这个符号分割」指的显示位置 → 改回 | 并验证`。

**三层「没有痕迹」叠加**：
① **Ctrl-C 中断只写屏幕**（`catch (OperationCanceledException)` 里只有 `_conversation.Add(...)`）⇒ **事件流一条不写**；
② **reset 按设计丢事件**（旧卷归档，新会话从零开始）；
③ **交接件不带「最后一条用户消息」**（只带白板/草稿/游标）⇒ 新会话的白板写着 `solve: （无进行中任务）`，
   **它有理由相信**「没有进行中的任务、在等新指令」。

**修法**（不动协议，用既有的「运行时对模型说话」通道 `SessionEventKind.Hint`）：
① 中断当场**进流**一条 Hint（同会话内下一轮就看得见）；
② 交接件增 `LastUserMessage` + `LastTurnInterrupted`，`/start` 时作为**新流首条 Hint** 交给模型。

**通法**：① **「状态没变」与「状态在系统外变了」必须分开** —— 屏幕上的提示不算留痕，**模型看不见的都不算**；
② 跨会话的连续性只能靠**显式交接**（白板/草稿之外，还得有「上一句人话 + 它有没有答完」）；
③ 先复用**既有通道**（Hint），不轻易加新 KIND / 改协议 —— 通道已经有了，缺的只是往里写。

---

## 127. 「已写进设计的修法」只落到部分读者 ⇒ 同一族在真机复发（并发枚举活表 ⇒ SIGABRT）（2026-09-22 23:28，[WB] 作者真机）

**现场**（`~/.agentruntime/last-crash.txt` 自报，03:49 加的兜底 dump 首次立功）：
`InvalidOperationException: Collection was modified; enumeration operation may not execute.`
@ `LifecycleAggregator.Aggregate` → `SplitSession.CurrentLifecycle` → `Frame` → `WhileThinkingAsync`（思考态重绘拍）；
流末 = 一条 `RiskClaimed`（工具调用已留档、结果还来不及追写）⇒ **枚举途中被追加**，未捕获 → `SIGABRT`。

**病根**：坑 #88（2026-09-20）早就定了修法「跟线程读一律走 `Snapshot()`（当拍拷贝）」，但它**只落到了部分读者**：
帧渲染路径（`CurrentLifecycle` / `AppendToolLines`）与宿主读口（`HasToolOutcomeSince` / `LastRequest` / `LifecycleOf` / `PanelRouter`）
都还在直读 `Stream.Events`。同一族缺陷因此能在真机上复发。

**修法**：① 投影入口重载内部取 `stream.Snapshot()`；② 上述 6 处全改走重载 / `Snapshot()`；
③ 加**可执行闸门**：源码里再出现 `.Stream.Events` 就红 + 并发烟囱测试。

**通法**：① **改一条口径，必须枚举“谁在用它”** —— 修法不是一个函数，是**一张调用点清单**；
② 设计文档里写的口径，如果没有**可执行闸门**看着它，就等于没写（漏一处 = 全漏）；
③ 顶层兜坑 dump（异常全文 + 配置 + 流末）值得在每个宿主都装 —— 这次它直接把“哪个类、哪一行、哪个线程”指了出来。

---

## 128. 「工作期间敲的命令」被当成插话喂给模型 ⇒ 命令失效 + 白开一张卡（2026-09-22 23:3x，[WB] 作者真机）

**现场**：作者工作期间敲 `/result`（想看当前结果）⇒ 旧路径把它当**插话**发出去 ⇒ 模型回了一句「`/result` 收到…」，
而那句还进流成 `UserInput` ⇒ 生命周期聚合**白开一张卡**（`用户：/result`），把真正的任务叙事劈成两半
（`.stage1/stream-20260922-231302.jsonl` E218~E220：中断 Hint → `/result` → 模型的“收到”）。

**病根**：**同一句输入有两条入口**（正常提交 / 工作期间的插话），而命令判定只装在了其中一条：
`SubmitAsync` 走 `IsCommand` 白名单，插话路径（`_pendingSteer`）直接 `message = steer`。

**修法**：插话路径也过同一判定（`SplitSession.SteerIsCommand`）：命中名单 ⇒ 按命令执行；退出词 ⇒ 退出；
其余 ⇒ 插话（并脱一层 `//` 转义）。闸门：6 例理论测试。

**通法**：① **多条入口 = 多处判定**：一条规则只看一个入口，就等于只看了一半（与 #122「形状容差不足」同族）；
② **命令永远不该进流**（进去就变成“用户说过的话”——卡、水位、叙事全被污染）；
③ 修一条输入规则时，先把**所有入口列出来**（提交 / 插话 / 管道 / 演示脚本），再逐条装同一份判定。

---

## 119. 自称「只读」的收尾检查脚本，实际写了账本（2026-09-22，[WB] 补收尾时当场抓到）

- **现场**：跑 `python3 scripts/knowledge-repo.py --out knowledge --closeout`，本意只是「看一眼三态」——
  帮助文案写明 `--closeout 三态判定（只读、不写、不交互）`。跑完 `svn status` 却发现 `knowledge/.ledger.json` 变成 **M**：
  它往 `checks` 数组追加了一条 `state: C / action: reported-only`（时间戳可查）。
- **实害**：0（记录本身正当，本就该留痕）。**真害在口径**：一处「自称只读」的动作**改动了待提交集合** ——
  收尾人若按「只读不写」的心智模型去 `commit`，就会把一笔**没审过的改动夹带进去**。
- **与 #86 同族**（同名同姓第二次）：`--closeout` 的**判定**是只读的，**记账**却写；文案把两件事说成了一件事。
- **修法**：① 帮助文案改为「只读判定 **+ 追加一条审计记录到 `.ledger.json`**」；
  ② 收尾流程把账本改动**显式列入**待审清单（不许当噪声滤掉）。
- **通则**：**「只读」是一个可能说错的声明，不是一个可以相信的保证。**
  判据用 `svn status` 对账（**动没动，看工作副本，不看它怎么说**）；凡日志 / 账本类追加写，**必须先在文案里承认**。与 §十·31（验证先验量法）、#84（时间戳 ≠ 提交状态）同族。

---

## 120. 「停下来问人」却没发 `[NEED-USER]` ⇒ 卡永停在 `● 进行中`（看起来像卡死）（2026-09-22，[WB] 作者真机报）

**现场**：作者让 [WB] 补收尾；它干完一批后**用自然语言问了一句**
（「请裁决：**L3 切条现在重建吗？** 若要，请给 API key 文件的路径」）——**没发终局块**。
于是生命周期卡停在 `#3 ● 进行中 24 轮`，task 钟停表、显示不变；作者看到的是「一动不动，也不是得解」。

**取证（三条，可复算）**：① `--lifecycle-show` 回放：`#3 ● 进行中`；
② 用量账本最后一笔 = **Turn 37 @ 17:22:15**（之后无新轮）⇒ **没有在途请求**；
③ `lsof -p <pid> -a -i` 空 + 主线程采样在空闲等待 ⇒ 进程活着、在等人，不是卡死。

**根因（协议层）**：协议第 6 条只写「**要人**就发 `[NEED-USER] <what you need>`」——但模型把「我来问你一句」
当成了**普通 prose**，没当终局。而 §六对「不该终局的情况」（可能造成巨大损失）写了负向口径，
**唯独没写「停下来问人 = 一种终局」**。⇒ 卡遂永不收口（`IsSettled` 只认 Done/NoSolution，人看着就像悬住）。

**修法（已并入协议 v20，作者 2026-09-22 21:5x 令）**：第 6 条补一句 ——「**停下问人本身就是终局 ⇒ 用 `[NEED-USER]`**；
自然语言问句不能替代它（没人知道轮到你说话）」。与 #118 同族、**同一个 v20（两次编辑之间无真机跑 ⇒ 没多付一次冷启）**。

**通则**：① **「停下来问人」是一个终局态，得有自己的令牌**；只给「要人」一个正面词、不给「何时用它」的口径，
模型就会把它当礼貌用语漏掉；② **“界面一动不动”与“卡死”长得一样** ⇒ 诊断先分三件：还有没有在途请求（账本/连接）、
进程是否在等输入（采样）、卡状态是不是无终局（回放）。


## 131. `svn add/commit` 报 `E205000: --trust-server-cert-failures requires --non-interactive`（2026-09-22，[WB] 独立成区）

- 证书 flag 不能单独给：`--trust-server-cert-failures=...` 必须与 `--non-interactive` **成对**。
- 症状：`svn add` 与 `svn commit` 全失败，而 `svn status`（没带 flag）正常 ⇒ 看着像「代码没问题」。
- 做法：模板统一写 `svn <cmd> --trust-server-cert-failures=unknown-ca,cn-mismatch,expired,not-yet-valid,other --non-interactive`；进脚本时**别把 flag 存进 shell 变量再用引号包**（会被当成一个参数 ⇒ 另一个 E205000）。

## 123. 判「目录是不是 SVN 工作副本」不能看 `<dir>/.svn`（2026-09-22，[WB]）

- SVN 1.7+ 采用**单根**布局：只有**工作副本根**有 `.svn`，子目录没有 ⇒ `[ -d "$DIR/.svn" ]` 必然误判「不是工作副本」。
- 正解：`( cd "$DIR" && svn info . >/dev/null 2>&1 )`。

## 124. `svn export <src> <dst>`：`<dst>` 已存在 ⇒ 静默不落盘、退出码 0（2026-09-22，[WB]）

- 看输出像「成功」，实际 0 文件（`find | wc -l` = 0）。
- 做法：① 要复刻就 `svn export --force`；② 验收用**独立计数 + diff**（源 N ↔ 新 N），别信退出码。

## 125. 改了归属、旧通道没封：本地活版仍挂旧 URL（2026-09-22，[WB]，未处置）

- 现状：活版 `whitebox/workspace` 的 `.svn` 仍指向旧 URL `.../projects/AgentRuntime/whitebox/workspace` ⇒ 在该目录里跑裸 `svn commit` 会**静默落旧存档路径**（违反新口径）。
- 作者令「不要动」，故仅记录；当前所有提交都走 `wb-sync.sh`（在存档区一侧执行）。
- 抽象：见 `KNOWLEDGE.md` §十·46。

## 132. 「屏上报什么」不能用配置反推，必须问「真的装进去了吗」（2026-09-22，[WB]）

- 现场：作者要顶层 TUI 在区栈与白板之间加一行「专家」，显示 **R1 里实际装载的专家知识分类**。
- 第一版按「冻结区 Expert 段」（段 id `knowledge.expert.<域>`）取 ⇒ 真语料 `frozen-private` 只有 `*_/global.md`，**一个 expert 文件都没有** ⇒ 永远显示「（未装载）」，把作者记得的分类全漏掉。
- 真相：真机 R1 里的分类来自**技能常驻层 L1+L2 的域**（走冻结区来源装饰器接进来，同样进 R1、同样进前缀指纹）；`SkillResident.L2` 的域正好是 `KnowledgeDomains` 那套 id（真机 10 个）。
- 反面同样危险：`FrozenSelection` 空选择会**展开成 17 个预设域** —— 若按「配置里选了哪些」渲染，就是**谎报装载**（配了但没内容 = 没装载）。
- 抽象：屏面/报表读的是**事实**，唯一可接受的来源是「真组装产物」（trace / 已装来源的只读出口）；配置、预设清单、期望列表都只能用来**校验**，不能用来**显示**。
- 实现锚点：`StackPanel.ExpertDomains(layers, resident)`（两个来源取并集，次序回 `KnowledgeDomains`）+ `FrozenZoneModuleBase.ContentSource`。

## 133. 工具块的 JSON 里塞长内容/特殊字符 ⇒ 被截断成非法 JSON，白烧一轮（2026-09-22，[WB]）

- 现场：用 `write`/`exec` 的单行 JSON 传一段含反引号与 `<域>` 的提交消息 ⇒ 工具面报「参数不是配平的 JSON 对象」（被按字符上限截断）。
- 同轮另一次：`svn commit -m "..."` 里的反引号被 **shell** 当命令替换 ⇒ `syntax error near unexpected token`（与坑 #50 同族：长消息别走命令行）。
- 做法：长文本一律走 **heredoc（`<<\'EOF\'`，单引号不展开）写文件 + `-F <文件>`**，或直接用 `edit` 改文件；命令里**不要**出现反引号/`$( )`。
- 顺带：`--trust-server-cert-failures` 必须与 `--non-interactive` 同时给（只给前者报 E205000）。

## 134. 提交前先 `pwd`：`cd ../../..` 的算术会把路径拼成 `projects/projects/...`（2026-09-22，[WB]）

- 现场：从工具 cwd（`.../projects/AgentRuntime/src/AgentRuntime.Tui`）出发 `cd ../../..` 落在 `.../software-company/projects`，于是显式列的路径变成 `projects/projects/AgentRuntime/...` ⇒ `svn: E155010: The node ... was not found`。
- 与坑 #77/#71 同族：**相对路径只在某一个基准下成立**。做法：提交前先 `pwd`（或直接用仓库根的绝对路径），显式列路径；提交后 `svn info` 核对版本号。

## 127b. 提交日志写坏了**改不了**：服务端没开 pre-revprop-change（2026-09-22，[WB]）

- 现场：坑 #133 的反引号被 shell 吃掉两处（r813 的日志里「显示从 变为」缺了示例），想用 `svn propset --revprop -r 813 svn:log -F` 修正 ⇒
  `svn: E175008: Repository has not been enabled to accept revision propchanges`（本仓库无 `pre-revprop-change` 钩子）。
- 结论：**提交日志是一次性产物** —— 写之前必须先在本地文件里成稿（`cat > /tmp/x.txt <<'EOF'`）、用 `grep -c 反引号` 自检、再 `-F` 喂给 svn。
- 补救只能靠**追加一版**（多一条提交说明），别指望事后改日志。

## 129. 分类名会与实际内容漂移：显示层要按「实际装了什么」取名，且名字只上屏（2026-09-22，[WB]）

- 现场：作者看屏问「ops 是什么分类？security 是软件相关的安全还是什么具体的安全，没理解」——两处都问对了，因为**名字骗人**：
  `ops` 的预设长名是「运维/部署」，实际那 45 行装的是流程/交付/发版/上架/归档/部署；
  `security` 的长名是「安全/隐私」，实际 11 行全是凭据/权限/授权，**零**软件漏洞类。
- 危险在两头：名字太窄 ⇒ 人以为「不是我这类事」（漏用）；名字太宽 ⇒ 人以为「里面有软件安全」（误信）。
- 做法（与坑 #132 同族）：**屏面名按实际内容取**（`ShortName`），拿不准就先 `python3` 把该域的行按 skill 列出来看，别照抄 id 或长名。
- 硬约束：**名字只上屏，绝不动 id**。id 是账本（段 id / 配置 / 派生 `domain` 字段 / 常驻层 `[域]` 标签都由它构成）—— 改 id = 动冻结前缀 = 缓存整体归零，换不来任何确定性收益。
- 落地形状：`KnowledgeDomain(Id, DisplayName, ShortName)` 三名并存；屏面用 `Label`，未知名回落 id（绝不编名字）。

## 130. 「记进坑集」防不了复发：已记的错因与真机复发的错因**不是同一个**，且 L4 坑集不进模型上下文（2026-09-22，[WB] 作者质问）

- **来源（作者）**：「JSON 书写格式不合法问题，已经出现了很多次了，为什么没有写入踩坑集之类的，来避免下次重复出错呢？」
- **查证结果：写了，三次，还做了三处工程化** ——
  ① **#74**（2026-09-17）：`exec` 的参数键猜成 `cmd` ⇒ `ToolArgs`「未声明键即拒」；
  ② **#113**（2026-09-22）：协议 v16 的紧凑键清单被照抄成**没引号的 JSON** ⇒ `ToolArgs.Parse` 拒（11 轮里 3 轮纯浪费）；
  ③ **#133**（2026-09-22）：工具块 JSON 里塞长内容/反引号 ⇒ 按上限**截断成非法参数**。
  配套：**协议区 v18**（形状说死 + 带引号完整示例 + 键清单降级为附录）、**拒绝话术** `ToolRunner.ToolFaceContract` 补带引号示例、**测试** `ToolFaceTests` 断言「JSON 的键要带引号」；抽象层也已有 **§十·39**（紧凑写法是最强的示范）与 **§十·49**（工具面长文本一律走文件）。
- **但真机复发的是另一类。** 用**真解析器**（`ToolReport.Parse`）跑 5 份真机夹具（模型原始回复、逐字节）：**5/5 全 Malformed**；逐块定位 ⇒ 触发块都是**参数跨行、且 JSON 字符串里含真换行**。
  ⇒ **真因 = 模型把多行正文直接塞进 JSON 字符串（换行没转义成 `\n` 两个字符）**；**次因 = 解析器更早就放弃**（**空行** / 下一个块头 / **20 行上限**都会终止参数拼接）。
  ⇒ **长度不是原因**：同一份回复里 694 字符的**单行**完整 JSON 全部 `Ok`。**同族不同病**（已记的是「键名 / 引号」，复发的是「串内换行」）。
- **⚠️ 本条第一版病因写错了，保留以示纪律**：原写「复发的错因 = 配平 / 截断（长度层）」—— 那是**我自写脚本**（仿配平算法）给出的假象（它无条件向后拼接、不判「已配平即停」）。换 **真解析器 + 真 `JsonDocument`** 才定到「串内换行」。**本轮两次自写探针全部作废**（§十·31 同族：量法错会伪装成结论）。
- **根因（三条，按可操作性排序）**：
  1. **拒绝话术答错了成因**：模型被拒后看到的仍是「**JSON 的键要带引号**」，而它犯的是「配平」⇒ 话术**教不会它怎么改**，只能靠它自己猜（实测第 4 轮才改对）。
  - 2. **L4 踩坑集不进模型上下文**：`PITFALLS.md` / `memory/` / `handoff/` 是**素材层、不装载** ⇒ 写在里面的东西，**写工具调用那一刻根本看不见** ⇒ 「写进踩坑集」这条路**在机制上就防不了复发**。真正能防的只有三处：**协议区**（冻结前缀，代价 = 缓存整体归零）/ **拒绝话术**（0 缓存代价，且**在错的当刻**出现）/ **解析器容错**。
  - 3. **「软失败」没有成本可见性**：一次被拒 = **白烧一轮 = 整份上下文重发**（同族 §十·37：轮数是成本的乘数），但这个成本**不上屏、不记账** ⇒ 从决策视角看「写错的代价 ≈ 0」，于是高风险写法（长内容塞单行 JSON）每次都被选中。
- **自我更正（量法错会伪装成结论，§十·31 同族）**：本轮第一次量数得出「16/21 是 JSON 类」，**作废** —— 那是 `grep -c 'tooldenied'` **命中了本会话自己的命令文本**（我自己的探针把关键字写进了流里）。改用「**按事件类型**判 + 只认 `kind`」后 ⇒ **5/5**。**数字变小 ≠ 问题变小**：5/5 说明的是「**这一族一旦发生，就是同一种**」，比 16/21 更能直接指向修法。
- **修法（作者 2026-09-23 令「A+B 做实验」，均已落码）**：
  - **A（0 缓存代价）**：`ToolRunner.ToolFaceContract` 补一条 —— **「多行正文不要塞进单行 JSON（字符串里出现真换行就不是合法 JSON ⇒ 要写长内容请用 `edit` 工具改文件）」**。
  - **B（0 缓存代价）**：**报告「解析器观察到的事实」** —— `ToolReport.Diagnose`（串内真换行 / 引号没闭合 / 缺右大括号）**+ 为什么停**（空行 / 块头 / 20 行上限）+ `ToolArgs.RawNewlineHint`。**不猜病因、不纠错**（纠错会动「参数即模型原文」的口径 ⇒ 要单独拍板）。
  - **C（有缓存代价）**：协议区补纪律 —— **只在 A/B 实证有效后才动**（§十·36：改给模型看的东西 = 重养行为，必须 A/B）。
  - **闸门**：`解析_真机形状_多行正文塞进JSON_必须报出_空行与换行_这一事实`（正反两面）+ `拒绝时_工具面一次说全` 增断言（钉住新纪律）。
- **A/B 实测结果（2026-09-23 · PTY 真机 3+3 跑 · 可复算）**：
  - **B ✅ 确定性收益（不依赖模型）**：同一批 5 份真机夹具，旧解析器只说「参数不是配平的 JSON 对象」；新解析器给出**可照着改的诊断**——「JSON 的**字符串引号没闭合**；参数还没写完就遇到了**空行**（空行会终止参数）…修法：多行正文不要塞进单行 JSON」。
  - **A ⊘ 本轮测不出**：基线 3 跑 **`denied=0`** —— 模型把多行正文**正确转义成 `\n`**，一次没滑倒 ⇒ **条件未触发 ⇒ 无差异可测**。换过第二版题（更长、要求含空行）仍不触发。
    **结论不是「A 无效」，而是「这一族是偶发滑倒，6 跑抓不到」**（依 §十·22：测不出来先怀疑量法 —— 已换两版夹具）。
  - **已决（作者 2026-09-23 令「按推荐走」）**：**A 保留、标注未证** —— 它是「**仅在已经被拒之后**才出现的一句话」，0 缓存代价、只增信息（不删、不弱化原有约束）；下次真机再被拒时按现场复核，**届时仍无用再删**。
  - **⚠️ 反面纪律**：本轮**没有**把 A 写成「已生效」——**测不出就是测不出**（§十·31、#117 同族：无实测即无可测收益）。
- **通则**：**「记下来」≠「防得住」** —— 防复发的判据是「**写错的那一刻，模型看得见什么**」。凡希望模型改行为的知识，必须落在**它每轮都能读到的地方**（协议区 / 话术 / 工具结果），落在素材层等于**只留给人看**。与 #93（假绿）/ #116（改结果会改行为）同源：**先问这条知识在哪被消费，再问它写没写。**

## 135. 工具路径的基准是**工具 cwd**，不随 shell 的 `cd` 改变（2026-09-23，[WB] 收尾时连栽两次）

- **现场**：shell 里先 `cd .../whitebox/workspace`，然后调 `edit {"path":"KNOWLEDGE.md",…}` ⇒ 报
  `文件不存在（.../src/AgentRuntime.Tui/KNOWLEDGE.md）` —— 工具**忽略**了上一条 `exec` 里的 `cd`，直接从**工具自己的 cwd**（`src/AgentRuntime.Tui`）解析相对路径。
  **同一次收尾，`read` 也栽了同一处**（`read {"path":"../tests/..."}` ⇒ 落到 `src/tests/...`）。
- **根因**：`exec` 的 `cd` 只作用于**那一条命令自身**的 shell 进程；`read` / `edit` / `write` / `list` 走的是**工具面**，基准 = **工具 cwd**（本机 = `src/AgentRuntime.Tui`）。
  两个 cwd 是**两套基准**，看起来像同一个。
- **症状极具误导性**：报错里给出的那个「实际路径」才是真相（它把工具 cwd 拼出来了）；只看自己 shell 的 `pwd` 会以为路径没错。
- **口径**：
  1. 工具路径**一律从工具 cwd 起算** —— 工作区文件写 `../../whitebox/workspace/...`；
  2. 判不准就先 `read {"path":"."}` 或看报错里给的**基准目录**（工具已经把它印出来了）；
  3. **替换后必须逐处 `grep` 复核**（本轮第一次三处改动**全落空**，靠核对才发现）。
- **处置（2026-09-23 夜，已落地）**：`RuntimePaths.PinToolBase` 在真入口 `RuntimeHost.Boot` 里把**进程 cwd 钉到活版**，并注入 `WB_ROOT`（代码仓）/ `WB_LIVE`（活版）/ `WB_ARCHIVE`（WB 存档区）三个绝对根，工具子进程继承；不配 / 不存在 ⇒ **不猜**（原样不动）。
  ⇒ 闸门 = `tests/AgentRuntime.Tests/ToolBaseInjectionTests.cs`（含**端到端 exec 用例**：子进程 `pwd` = 活版 · 相对路径读得到 · `$WB_ROOT` 看得见）。
  ⇒ **口径**：跨目录一律用这三个变量拼**绝对路径**，别再靠 `cd ../../..` 的算术。
- **同族**：坑 #71（[WB] 侧路径基准两处暗礁）· #77（启动命令的隐含前置）· §十·50（相对路径只在某一个基准下成立）。
- **通用**：**凡是「看起来像同一个目录」的两个基准，都要当成两回事** —— 报错里那个绝对路径，就是系统在告诉你它用的是哪一个。
- **附带（本文件既有问题 + 我自己的第三次栽跟头，2026-09-23 顺带发现）**：`PITFALLS.md` 的节号已出现**重号**（`131`/`132`/`133` 各有两条）。**本轮新增这条时，我连撞三次**（先写 131、再 132、再 133 —— 全被占）⇒ 这正是坑 #38（「节号当 id 会撞/漏」）的**活体重演**。
  ⇒ 口径：**写新条之前先查最大号**（`grep -o '^## [0-9]*\.' docs/PITFALLS.md | grep -o '[0-9]*' | sort -n | tail -1`），**再 +1**；引用旧条时**带上日期或标题**，别只说号。
  ⇒ 通则：**编号不是身份** —— 任何「靠人手工递增」的号，都会在并发 / 长文件里撞车；能自动分配的（如事件流的 `Seq`）就别手写。

## 136. 「只读探针」用了不被识别的参数 ⇒ 脚本照跑「写盘」分支（2026-09-23，[WB] 收尾时当场踩到）

- **现场**：收尾终检时想「先看看脚本怎么用」，执行 `python3 scripts/knowledge-index.py --help` ⇒ 屏幕回的是 `已重建索引区：BULLETIN.md` —— 它**不是读帮助，它把文件改了**。
- **根因**：脚本只认 `--check`（`CHECK = --check in sys.argv`），**其余参数一律被忽略** ⇒ 落到默认的「重建（写盘）」分支。`--help` 既不在白名单、也不报错。
- **为什么危险**：拿「看一眼用法」当**只读动作**，实际产生了写；副作用恰好把该重建的索引补上了 ⇒ **没报错、没异样感**，最不易察觉的一类。
- **口径**：
  1. 只看用法/状态 → 先 `read` 脚本头部或 `grep` 关键分支，**别拿未知参数去试**；
  2. 自写 CLI：**未知参数应当报错退出**（fail-closed），默认动作是「写」的必须给 `--dry-run`；
  3. 「探针」不等于只读 —— 先确认它**读什么、写什么、什么条件下写**。
- **同族**：坑 #86（自称「只读」的工具其实会写）· §十·31（验证先验量法）。
- **附带（本次收尾真正的异常：流程缺一步）**：本会话 BULLETIN **正文**加了 09-23 条目，却**没有重跑生成器** ⇒ 索引区锚点停在旧正文（`--check` 红），随后 `wb-sync.sh push` 把**过期索引**推到了存档区（r843）。
  ⇒ 口径：**索引区是派生物，正文一变必须重跑**；收尾清单里 `python3 scripts/knowledge-index.py --check`（应变绿）要**排在 push 之前**，并作为 push 的前置闸门（**红则不 push**）。本次已补跑并 push = **r850**。

## 137. 「写成功」的判据 = 落点 + 体量 + 哈希；正文写进共享临时区必须**当场警告**（2026-09-23，[WB] · 01:15 收尾丢了一层 L4）

- **现场**：2026-09-23 01:15 收尾会话把四层正文（`KNOWLEDGE` §十·53 / 坑条 / 当日 `memory` §十 / 水位块）**先写 `/tmp/{sec10,sec53,pf137,wl_add}.md`**，再用自制脚本 `/tmp/wb_all.py` 回灌仓库 —— 脚本 `SyntaxError: EOL while scanning string literal`（第 59 行）⇒ **四层正文一处没进仓库**；而同一时刻 `tools/closeout.py --check` 是 **block 全绿**（"该做没做"不是可重算项）。
- **根因（两层）**：① 写入结果里**参数回显排在结论之前**，写大正文时结论被淹掉 ⇒ 看起来"成功了"；② 落点在共享临时区**没有任何提示**，工具照写不误。
- **做法**：`write` / `edit` 的结果**先说**「绝对落点 · 字节数 · 新建/覆盖/逐字节相同 · 前后 sha256」，参数回显挪到后面；落点在 **`/tmp` · `/var/tmp`**（含 macOS `/private` 真身）⇒ 追加 ⚠️「正文 / 知识**不该写这里**」；落点上级目录是本次**新建**的 ⇒ 追加 ⚠️（相对路径按**工具 cwd** 拼出，见坑 #135）；`edit` 文件不存在 / `write` 目标是目录 ⇒ 失败话术**点明基准目录**。
- **判据（可复算）**：`tests/AgentRuntime.Tests/ToolWriteEvidenceTests.cs` 六条，正反两面：新建 / 覆盖 / 同内容、目录目标、`edit` 缺文件、共享临时区警告、"结论必须排在参数回显之前"。
- **号说明**：本号原被 01:15 会话为「闸门全绿 ≠ 收尾完成」预留（内容仍在 `/tmp/pf137.md` 与 `/tmp/sec53.md`）⇒ 那条重跑时接 **#138**。
- 同族：坑 #130（记了 ≠ 防得住）· `KNOWLEDGE` §十·51。

## 138. 闸门复述被测物的算式 ⇒ 自洽≠正确：注入的 `WB_ARCHIVE` 少数一层，闸门永远绿（2026-09-23，[WB] 收尾复核当场查出）

- **现场**：`RuntimePaths.PinToolBase` 从活版往上 `Path.Combine(live, "..", "..", "..", "whitebox-workspace")` ⇒ 3 层 = `software-company/projects/whitebox-workspace`，**该目录不存在**；真存档区在 `software-company/whitebox-workspace`（**4 层**）。
- **为什么一直没红**：守护测试 `注射_把_cwd_与三个根钉到工作区` 的期望值**照抄了同一句算式** ⇒ 实现怎么错、期望就怎么错，**永远绿**（**自洽 ≠ 正确**）。同机 `tools/closeout.py` 与 `scripts/wb-sync.sh` 各自**自定位**算的都是对的（4 层）⇒ **同一常量两套算法，只有进环境的那个错**。
- **判据**：① 闸门期望值必须**独立来源**（真实布局常量 / 直接问运行时 / 校验存在性），**不得复述实现里的表达式**；② 路径类再加一条**存在性**断言 ——「拼出来的路径」只是字符串，**存在**才是事实；③ **新加的闸门必须当场真跑一次**（本例 cwd 断言若被跑过就早该红）。
- **实测**：修完 `RuntimePaths.cs` ⇒ `dotnet test --filter ToolBaseInjection` **3/3 绿**（改前 1/3 红）。
- 同族：坑 #62（只验「文件在不在」= 假绿）· §十·30（闸门要有牙）· §十·53。

## 139. 判「是不是同一个目录」别比字符串：macOS `/var` → `/private/var`，同一目录两种拼法（2026-09-23，[WB] 修 #138 时当场暴露）

- **现场**：`Assert.Equal(Path.GetFullPath(live), Directory.GetCurrentDirectory())` —— 前者 `/var/folders/…`（`Path.GetFullPath` **只做词法规范化、不解符号链接**），后者 `/private/var/folders/…`（`getcwd(3)` 返回**真身**）⇒ 同一个目录，断言**恒假**。
- **做法**：改**语义判据** —— 在活版放一枚标记，再**按相对名**读它（能读到 ⇒ cwd 真在活版）。不依赖路径拼法，也不依赖平台符号链接规则。
- **通用**：路径比较的三种安全形态 = ① 语义行为（相对名读写）② 两侧都 realpath ③ 存在性/真值。**字符串相等只对「拼法恰好一致」负责**。
- 同族：坑 #135/#137（基准是隐式状态）· §十·54。

## 140. 闸门全绿 ≠ 收尾完成：机器判据只覆盖**确定性**项，四层内容义务仍会静默掉队（2026-09-23 现场 · 2026-09-24 [WB] 补录）

- **号说明**：本条原被 2026-09-23 01:15 会话预留为 **#137**（正文当时落在 `/tmp/pf137.md`，未进仓库，见 #137 的「号说明」）；该号后来给了「写成功 = 落点 + 体量 + 哈希」⇒ 本条补录时接 **#140**。
- **现场**：`[OC]` 侧 01:1x 代做收尾流水线（r852~r868），机器项齐活 —— `python3 tools/closeout.py --check` **block 红 0 项**、存档收敛点在册、活版↔存档区 **53/53 零漂移**。但对照**四层义务**（L1 法则 / L2 问题地图 / L3 处理单元 / L4 素材），**只有 L3 侧的工具与铁则改了，L4 三处正文没跟上**：
  - `memory/2026-09-23.md` 止于 §九（收敛点 r850）—— 流水线这一夜**无当日正文**；
  - `memory/收尾水位.md` 叙事无流水线条目（只有机器收敛点行）；
  - `MEMORYINDEX.md` 无 09-23 流水线时间线行。
- **根因**：`--check` 的判据**全部是确定性可重算项**（派生一致 / 副本一致 / 计数 / 指纹 / 未提交）；**「该写而没写」的内容义务，机器判不出来**（判断类归模型）。⇒ 闸门绿只证明**已做的那部分没矛盾**，不证明**该做的都做了**。
- **口径**：
  1. **闸门（block）管「做了的事对不对」；编排管「该做的事有没有做」** —— 两者不可互相顶替；
  2. 收尾铁则 **step 0 已把四层列成内容义务**（r857）—— 但**「列成文字」不等于「会执行」**（同 `KNOWLEDGE` §十·22：可选措辞 ≈ 永不执行）⇒ 需要**显式清单 + 逐层打勾**，而不是散在散文步骤里；
  3. 判据：**换一个不知道来龙去脉的人照清单走，能复现同一收尾** —— 清单缺一步，就看得出。
- **落地（2026-09-23 之后已补）**：`--check` 增第 11 项 **`四层存在性`（block，应有集合）** —— 以「最后活动日」为锚，要求当日 `memory/<日期>.md`（存档区 + 活版，且 ≥300 B 非占位）· 水位有当日段 · `MEMORYINDEX` 有当日行。**它管结构性存在（抓「整层掉队」），抓不到「某段正文没写进去」** —— 后者要靠写入工具回显落点（见 #137）。
- **同族**：坑 #93（判据数「有没有文件」⇒「应该有而没有」看不见 · 假绿）· §十·30（闸门要有牙）· §十·51（记了≠防得住）。一次异常同时命中了「假绿」与「义务缺步」两条。

## 141. 闸门的**可达性前置**：判据先要能拿到读数，否则「挂死」会伪装成「慢」（2026-09-24，[WB] 收尾实跑当场）

- **现场**：`tools/closeout.py` 的 `run()` 是裸 `subprocess.run(...)`，**无 `timeout`**；而闸门里有三处**必须问远端 URL**（`tree_rev` / `archive_rev` / `last_activity_date`）。2026-09-24 上午链路半死：`svn update` / `--check` **挂住 1.5 分钟无输出**（`libserf` 卡在 `kevent`），最后靠 `/tmp/svnshim/svn`（**只读**超时垫片）才读到读数。卡点在**连接建立**，不是请求超时 ⇒ 调 `servers:global:http-timeout` 没用。
- **同族第二处**：旧 `tree_rev` 在远端查询失败时**回落到本地 `last-changed-revision`** ⇒ 离线时返回**陈旧 base 号**；而这个数正是 `--close` 写「收敛点」的操作数 = **凭空编造一个收敛点**。
- **口径**：
  1. 任何闸门的**第一条不变量**是「读数拿得到」——远端/外部查询**必须带超时**；
  2. 超时**响亮红**（rc=124 + 可读起因），**绝不挂死**（挂死看起来像"慢"）；**绝不回落到陈旧本地值**；
  3. 只读诊断垫片（假 `svn` / fail-fast wrapper）**只用于排查**，用完即弃 —— 它不是修法，也**不许**为了让它跑而改闸门。
- **判据**：`--check` 新增 **`可达性前置`（block）**；`--selftest` 用「假 svn 永远不返回」证明**限时返回且红**（旧代码在这一点上会挂死）。
- 同族：§十·15（验证先怀疑量法）· 坑 #136（假只读）· §十·56。

## 142. 「白名单 + 子串 grep」式判据 = **判据比意图弱**（2026-09-24，[WB] 收尾实跑 · 两处实例）

- **实例一（范围太窄）**：`活版 ↔ 存档区` 只比**白名单**（六件 + 活版 `skills/**.md`）⇒ 白名单外的活版文件**改而不报**。实证：`knowledge/.ledger.json` 活版 126,447 B / 存档区 126,146 B，**两道闸门都不管**。**修法**：改**整树逐件**比对（实测 529 件），只排除 `.svn` / `.DS_Store`（等价于 `scripts/wb-sync.sh status` 的 `diff -rq`）。
- **实例二（深度太浅）**：`四层存在性` 原先只 grep「正文里**出现**日期字符串」⇒ `MEMORYINDEX` 的**版本行**（`v2.5 · 2026-09-24`）就把旧判据满足了 —— **时间线行漏写照样绿**；水位同理（日期出现在正文别处即算过）。**修法**：要求**水位有当日段行**（`- **<日期>`）+ **索引「一、时间线索引」段内**有当日行（`| MM-DD |`）。
- **口径**：判据按「**应有集合 + 应有形状**」写，别按「某字符串出现过」写；范围判据用白名单时必须先答「**不在名单里的怎么办**」。
- **判据**：`--selftest` 新增三条**决定性**负例（白名单外的活版改动 / 索引只有版本号 / 水位无当日段）—— 三条在旧代码下**都是绿**（假绿），这正是"没有牙"的实证。
- 同族：§十·16（应有集合）· §十·9（闸门要有牙）· 坑 #93（假绿）· §十·57。

## 143. 「了结即固化」与「终局后补一轮」相冲：定格判据要用「**还欠着**」（2026-09-24，[WB] 决策报告接线）

- **现场**：协议 v21 第 12 条要求「终局后宿主**请求一次**决策报告」⇒ 报告在**下一轮**才到。而 L2 的「了结即固化」在**终局那一刻**就 `_cardIndex = null` ⇒ 报告**没处落**（实测现象：卡上永远不出现「决策报告」，而流里明明有那份报告）。
- **根因**：定格判据写成了「**已请求**」—— 而请求是在本次 `LandTurn` **之后**才发的（那一瞬 `ReportRequested` 还是 false）⇒ 判据**永远为假**。同族：任何「先定格、后补产出」的流程都会踩。
- **口径**：定格判据用「**还欠着**」（`自动接续开 && 报告文为空`）—— 只要后续产出还没到，就不许定格；产出一到（**任何文本**）立刻固化。
- **判据**：`DecisionReportFlowTests` 三条 + `LifecycleCardViewTests` 三轮卡闸门（`Turns` 仍 **3**，而模型调用 **4** 次）。
- **同族**：§十·63（关账那一刻按真实状态重画再冻）· 坑 #140（该写而没写，机器判不出）。

## 144. 协议写的形状与解析器认的形状**两处各写一套** ⇒ 静默归错（2026-09-24 · 真机取样当场抓到）

- **现场**：协议第 12 条写 `next [rec]:` / `found [blocked|failed|noticed]:`（**带方括号**），模型照写带括号；而 `DecisionReportParser` 只认 `next rec:` / `found failed:` ⇒ **带括号的 `found [failed]:` 被归成 `noticed`**（错类别）、`next [rec]:` **丢掉「推荐」标记** —— 不报错、不失败，只是**静默错**。
- **根因**：「协议说的」与「代码解析的」是同一件事的两半，却在**两处各写一套**；单测只测代码那一套 ⇒ 两边永远自洽。
- **口径**：**给模型看的措辞一改，就必须用真机回复验形状**（同题 ≥3 跑；成本实测 ≈**7k token/例**）；解析器对真机上出现过的**两种写法都要认**，并把真机回复**原样钉成夹具**。
- **判据**：`DecisionReportTests` 两条真机回放夹具（带括号 / 省括号各一）；限定词归一（去 `[]`/`（）` + 小写）。
- **同族**：§十·15（契约形状只有真机能验）· 坑 #103/#104（形状认不得就丢）· §十·38。

## 145. 窗口/预算写死一个数 = **零余量**：块上限之和一到就静默不装载（2026-09-24 · 报告块上线前）

- **现场**：自报块扫描窗口 `ReportScanLines` 写死 **64**；各段上限之和（报告 30 + 白板 12 + 草稿 16 + 工具 4）= **62** ⇒ **零余量**，多一行就把最外那一块压出窗口 —— 而压爆的形状是**静默不装载**（先例：窗口 5 行时 `[L3]` 被 6 行 `[TAIL]` 压出，2026-09-16 实测）。
- **口径**：窗口/预算按**常量表达式**写（`= 各段上限之和 + 余量`）⇒ 任一段上限一改，窗口**自动跟着变**；不写死数字。
- **判据**：`SkillLoadingTests` 的「窗口 ≥ 各段上限之和」不变量 + `DecisionReportTests` 满额三段同现夹具（报告与邻块都还在）。
- **同族**：§十·16（应有形状）· 坑 #92/#93（假绿）· skill `expected-set-gates`。

## 146. 口径改名只改「源」，代码/闸门/账本里的**同名旧义**继续每轮反向重教（2026-09-24 · [WB] L4 分层落地时当场踩到）

- **现场**：作者定「L4 归技能重型知识、原素材层让位」⇒ 顶层 `AGENTS.md` / `KNOWLEDGE.md` 改净了；但 `tools/closeout.py` 里闸门名仍是 **`四层存在性` / `L4 三件` / `check_l4_presence`**（指的其实是**素材区**那三件）。于是每次 `--check` 都在屏上打印旧义 —— **改名改了源，机器又把它教回来**。
- **根因**：一个词（L4）在**三处各有作者**：文档（口径）· 代码（闸门名/函数名）· 账本（`gate-ledger.json` 的 why）。只改文档 ⇒ 另两处成了「活的旧口径」，且**每轮可见**（比没改更糟）。
- **口径**：改名 = **扫到「文档 + 代码 + 账本 + 测试」四处**；判据是 `grep` 旧名**四处皆空**，且**负例自证**（`--selftest` 里三条引用旧名的夹具必须跟着换成新名、仍旧变红）。
- **判据**：`python3 tools/closeout.py --selftest` → **13/13 负例变红**（其中三条挂在新名「素材区存在性」上）；`grep -n 'check_l4_presence\|四层存在性\|L4 三件' tools/` 为空。
- **同族**：坑 #104（撤闸门只改行为没改文案 ⇒ 屏上还在承诺）· 坑 #66（改名要扫到量表与夹具两处）· §十·32（按派生规则查，不是按字面量查）。

## 147. 判成败却接管道 ⇒ `$?` 恒为 0（假绿）（2026-09-24 · 作者点出「该常驻，不是个人疏忽」）

- **现场**：本轮 [WB] 自证 `--selftest` 时写成 `python3 tools/closeout.py --selftest 2>&1 | tail -30; echo "selftest_rc=$?"` ⇒ `selftest_rc` **恒为 0**：取到的是 `tail` 的退出码，不是 `closeout.py` 的 ⇒ **selftest 失败也会显示 rc=0**。
- **根因**：管道把上游退出码吞掉（bash 默认返回**最后一个命令**的码，且上游失败不传播）；`set -o pipefail` 没开。同族形态：`cmd | grep -q x` 判「成功」、`cmd | head` 判「通过」。
- **口径**：凡靠退出码判成败处 —— ① `set -o pipefail`；② `${PIPESTATUS[0]}`；③ 不接管道。**纯显示**（只想截取几行）也要显式写明「此处不看退出码」。
- **判据**：`bash -c 'set -o pipefail; false | tail -1; echo $?'` → **1**（不写 pipefail → **0**）。本轮两次活教材同型（`| tail -30` 后取 `$?`）。
- **同族**：§十·30（闸门要有牙）· §十·51（记下来≠防得住）· §十·58 · 坑 #40/#48（量法假阴性：**测不出来先怀疑量法**）。
- **落地**：`AGENTS.md` 铁则 7（常驻，零缓存代价；不进协议区）· `KNOWLEDGE.md` §十·58 · 本坑落点声明 `gate-ledger.json#147`。

## 148. **改口径 / 换架构**类任务的完成判据是**覆盖率（N/M）**，不是「机制已存在」（2026-09-24 · 本会话自己踩的）

- **现场**：作者令「本系统不应该存在 SKILL 了，应一并抽象到 L1 L2 L3 L4；**L4 放 SKILL 内部非常重的知识体系**」。我建好了 **L4 契约 + 生成器 + 顶层口径 + 首批 2 个产物**，就把它当**做完了**、报了 ✅。直到作者追问、我**量了数字**才发现：**L4 覆盖只有 2/44**（`SKILL.md` 仍是唯一真相源）。
- **根因**：把「**机制建成**」当成「**迁移完成**」。前者是**能力**（有一次就够），后者是**覆盖**（必须 N/M）。
- **口径**：凡「换架构 / 迁知识 / 改口径 / 全量重命名」类任务，**完成判据 = 覆盖率**，且必须**当场量出分母**（总件数）与分子（已迁数）。
- **判据**：交付里出现「已完成」字样时，同一行必须带 **N/M**；没有数字的一律视为**未完成**。
- **同族**：§十·22（写进协议 ≠ 模型会做）· §十·42（加了能力 ≠ 能力被用上）· 坑 #140（闸门全绿 ≠ 收尾完成）。

## 149. 量法错会**伪装成结论**：同一问题两种量法给出相反答案（2026-09-24 · 本会话两次）

- **现场**：① 判「SKILL.md 里有没有重型知识段」时，我的正则写成 `re.match(r'## (.+?)\s*$', s)` —— `\s*$` 要求**到串尾**，而 `s` 是**多行段** ⇒ **恒不匹配**，读数全 0，我据此差点断言「没有重型段」。② 同一问题按「**段标题**」量（真漏切 0）与按「**正文行数**」量（`svn-workflow` 20 行 vs 5 条，像漏切）**结论相反**。
- **根因**：**尺子**错了，不是被量的东西错了；而错尺子**不报错**，只安静地给出一个数。
- **口径**：① 量之前先问「**这个正则/口径能匹配到我想量的形状吗**」，用一个**已知正例**试一次；② 两种量法给出相反结论时，**先怀疑量法**（§十·31）；③ 判据要用「**应有形状**」写死（本轮：**看段标题、不看正文**）。
- **判据**：任何一次「量出来是 0 / 是满」的结果，都要能被**第二个独立量法**复现。
- **同族**：§十·31（「没复现」≠「没有」）· §十·52（别用仿写品验证被测物）· 坑 #40/#48。

## 150. 「空隙」也是内容：`strips` 没盖到的行**静默丢失**（2026-09-24 · 当场变红才抓到）

- **现场**：分家器按 `meta.strips` 的区间切条 —— 但 `strips` **未必覆盖全文**（实测 `context-index-maintenance`：条止于第 147 行 / 正文 **155** 行）⇒ 尾部 8 行**无声消失**，往返闸门变红才暴露。
- **根因**：把「**已声明**」当成「**已穷尽**」。区间集合**不完备**时，缺口看起来就像「没有内容」。
- **口径**：凡按「区间 / 清单 / 索引」搬运内容的生成器，必须先算**补集**（未被任何区间覆盖的行），并把补集**显式**当作内容处理（本轮：自成一拍）。
- **判据**：不变量必须写成「**Σ(所有条) == 源**」而不是「Σ(已声明的条) == 源」—— 后者对缺口**恒真**。
- **同族**：§十·30（闸门要有牙：正例 + 负例 + fail-closed）· 坑 #93（判「有没有文件」⇒「应该有而没有」看不见）。

## 151. id 撞号**静默取错**：比报错更贵（2026-09-24 · 当场抓到）

- **现场**：hybrid 路径的 id 公式 `len(ps) - hid + 1` 与 L3 号段重叠 ⇒ **`S-ctxidx-030` 在 `L4.jsonl` 里出现两次**。装载器按 id 取条时**静默取到错的那条**（不抛错、不告警）。
- **根因**：id 由**两处独立算式**生成，各自都「看起来对」，合起来才撞。
- **口径**：同一命名空间内的 id **由一个全局计数器分配**（`gid += 1`），禁止多路各自推号；**唯一性由构造保证**，不靠事后检查。
- **判据**：生成后**当场**跑一次唯一性检查（本轮：`L3 ∪ L4` 逐 id 计数，重复即报）—— 撞号技能数必须为 **0**。
- **同族**：§十·9（给不变量一个可执行的守卫）· §十·32（按派生规则查）· 坑 #81（唯一真相源被写两处）。

## 152. **空提交 rc=0**：改了没落地也照样「提交成功」（2026-09-24 · 本会话踩到）

- **现场**：`svn commit` 返回 **rc=0** 并打印「Committed revision」——但那次 **edit 其实失败了**（路径笔误多打一个 `}`）⇒ **一个字节都没提交**。与坑 #124（`svn export` 静默不落盘）同族。
- **根因**：`commit` 的 rc 只说明「**事务成功**」，不说明「**有内容被提交**」（空变更集也是成功事务）。
- **口径**：提交前先**校验前置**（本轮：`grep -q '<新内容>' <file>` 不通过就 `exit 1`，不提交）；提交后**自证**（`svn info <file> | Last Changed Rev` 必须**等于**刚拿到的 revision）。
- **判据**：`Last Changed Rev == 本次 revision` ⇒ 真提交；否则空提交。
- **同族**：坑 #124（export 静默跳过）· 坑 #72（空输出 ≠ 没变化）· §十·47（退出码 0 ≠ 做成了）。

## 153. `svn update` 会把「本机删了但没登记」的文件**原样恢复**（`!` ≠ `D`）（2026-09-24 · [WB] 收尾当场踩到）

- **现场**：`memory/test.md` 从活版用 rsync 删掉后，存档区 `svn update` 打出 `Restored … test.md` —— 文件**回来了**。此前 `svn status` 只显示 `!`（本机缺失），**不是** `D`（登记删除）⇒ 提交里没有它，「删掉」这件事根本没发生。
- **根因**：`!` 是**工作副本的本地状态**，不进变更集；`rsync --delete` / `rm` / `trash` 都**不等于**「登记删除」。于是「删」这个意图只活在本机，一次 `update` 就被抹掉 —— 而且**它不会报错**。
- **口径**：跨副本删除**必须** `svn delete --force <path>`（登记删除）再提交；`svn status` 里出现 `!` 就是**欠账信号**，不许当噪声滤掉、也不许用 `--force` 之外的花招绕。
- **判据**：两仓 `svn status` 的 `!` 计数 = **0**；删除类收尾以「提交后 `update` 不再恢复它」为准。
- **同族**：坑 #84（时间戳 ≠ 提交状态）· 坑 #119（「只读」是个可能说错的声明，动没动看工作副本）· 坑 #124（`svn export` 静默不落盘）· §十·31（先验量法）。

## 154. 「不铺原料」的口径**缺了「成品」一类** ⇒ 成品与原料一起被拍死（2026-09-24 · [WB] · 作者问「为什么题进不了报告」）

- **现场**：把测试题面写进 `[REPORT]` 块 ⇒ 人**看不到内容**。查实三处合起来挡：① 解析器**丢掉非白名单键**（`题:` / `A=3:` 全丢）；② 报告**无自由正文位**（`Brief` 固定 5 节）；③ 协议第 12 条写死 `no raw output`。
- **根因**：现行口径只分「原料」（不进）与「总结」（进），**没有第三类「成品」**（交付给人阅读/消费的内容本身）⇒ 一起拍死。
- **已存在的正确口径**：同仓库结果区 `ResultLines` 注释写着终局块正文「**决定型内容不截断**」、逐字全文上屏 ⇒ **仓库早就承认「决定型内容 ≠ 原料」**，缺口只在报告这处。
- **口径（两问）**：① 它是**为推出结论而读的过程产物**（原料）还是**交付物本身**（成品）？② 去掉它，人还能不能拿到要交付的东西？⇒ 原料不进，**成品必须能逐字进**。
- **落点**：待改项登记在 `whitebox/workspace/handoff/25-决策报告缺成品正文位-2026-09-24.md`；本轮替代走终局块正文（零缓存）。
- **同族**：§十·34（载体搬家要问「人在这张位置上读得到吗」）· 坑 #106（卡上只给首行 ⇒ 答复正文在屏上根本不存在）· §十·60（量法错会伪装成结论 —— 判据没错，用错了地方）。

## 155. **报告块无主**：写了 `[REPORT]`、却没有终局块 ⇒ 卡永停「进行中」，报告也没处挂（2026-09-24 · [WB] · 作者问「生命周期 #7 为何没有决策报告」当场抓出 = **坑 #120 的原样重犯**）

- **现场**：生命周期 #7 屏上「⚠ 未终局 / ● 进行中」、卡下也没有决策报告。只读回放（`--lifecycle-show`）读到硬事实：`E158` **61 行**、内容完整、**含 `[REPORT]`**，但整条回复**没有任何终局块**（`[DONE]`/`[NO-SOLUTION]`/`[NEED-USER]` 全无）。
- **根因**：**报告是「终局之后」的产物**，而那条回复把「事后报告」发了、却没发「结束声明」⇒ 宿主不认终局 ⇒ 卡悬空 + 报告无主。当时是**散文提问收尾**（「等你点头」），不是终局块。
- **为什么必然复发**：同一条回复里 `[TAIL]`（明说「没结束就不发终局块」）与 `[REPORT]`（明说「终局之后才交」）**两个块信号相反** ⇒ 按「有块 = 已收尾」处理就会两个都发（`§十·43`：每个块头只表示一件事）。
- **修法（已落地，零缓存）**：宿主在**错的当刻**判 —— 判据 = **既非终局轮、又非宿主请求的那一轮**时出现 `[REPORT]` ⇒ 当场纠正（`Hint` · `source=report-unowned`）。
  - ⚠️ **判据不能写成「有 `[REPORT]` 无终局块就拒」**：合法报告轮**本来就没有终局块**（`DESIGN-LIFECYCLE-BRIEF.md` §十三-derived #2）⇒ 那样会把**正确行为**判成错。故必须用 `_awaitingReport` 排除「我在要报告」的那一轮。
  - **来源标记必须与「报告请求」分开**（`report-unowned` ≠ `report-request`）：复用会被聚合器误读成**新的报告请求** ⇒ 下一轮被错认成合法报告轮。
- **闸门（正例 + 负例，两道都真跑过）**：`报告块无主_非请求轮写了REPORT却没终局块_当场纠正`（红例）+ `报告请求轮_没有终局块_不判无主`（正例 —— 没有它，判据过严会静默杀掉合法报告轮）。
- **同族**：坑 #120（「停下来问人」却没发 `[NEED-USER]` ⇒ 卡永停「进行中」）· §十·43（块头语义只表示一件事）· §十·51（「记进坑集」≠ 防得住 —— 本条就是 #120 的复发）· `§十·9`（守卫要有牙）。

## 156. **多步事务只记「完成」不记「意图」** ⇒「收尾被打断」与「根本没开工」在判据上不可分（2026-09-24 · 作者问「上次收尾都做完了吗」当场量出）

- **现场**：上一轮收尾**只走到一半**（交了素材），水位停在 **r939** 而实况已 **r952** —— 整整 **13 个提交、跨了一整个会话**没人发现。作者问起时，**没有任何闸门报红**。
- **根因（原理级）**：收尾 = **多步事务**（check ／ 写内容 ／ apply ／ 提交 ／ close ／ 再提交 ／ 再 check），而 **checkpoint（收敛点）只在最后一步写** ⇒ 中途被打断时，磁盘上**没有「已经开工」的痕迹**。看到的现象只有「水位落后」，而**落后既可能是「被打断」也可能是「正常还在跑」** —— 两个世界的形状一样。
- **为什么没被闸门拦住（第二层根因）**：`水位收敛点` 那个 block 闸门**明明读到了**「r939 → r952，13 个提交」，却把它判成 ✅，注释还替它开脱：*「正常（写完收敛点后还会有提交）」*。⇒ **读数有、判别无**：它看得见差距，却把「没走完」与「正常推进」混成一件事、用同一句话放行。
- **修法（本轮落地）**：判据改成**两个信号** —— ① 收敛点落后 **且** ② 会话**已收尾**（`~/.agentruntime/session.json` 的 `closed`）⇒ 红；只有① ⇒ 正常推进（会话还在跑）。读数拿不到就 **None（不猜）**，不造假的红绿。
- **同族第二条（本轮同场抓到）**：`L3 往返` 那条 `why` 写「**需远端 AI 重 emit**」—— 实测**一条 `--derive-only` 就绿了**（失败项是「派生不一致」）。⇒ **判据的注解会自我开脱**，而没人反测它 ⇒ 一条本来能自动修的欠账被挂成「等人」。
- **口径**：**给长事务记「意图」，不要只记「完成」**；判据必须能**区分「正常」与「异常」**，不许用同一句「正常」把两者一起放行。
- **同族**：§十·11（水位线）· §十·18（恢复点：只记位置不记内容）· §十·55（闸门全绿 ≠ 收尾完成）· §十·31（先验量法）· §十·51（记下来 ≠ 防得住）。

## 157. **JSON 串里的非法转义**：模型把 shell 正则搬进 JSON（`\|` `\(`）⇒ **整批工具调用被拒**（2026-09-24 · [WB] 真机 E095/E280 —— 工具块被拒的同族第**三个**病因）

- **现场（真机，可复算）**：`.stage1/stream-20260924-230628.jsonl` 的 **E095 / E280** —— 两条 `[TOOL] exec` 被整体拒，话术原文 `工具参数不是合法 JSON：'|' is an invalid escapable character within a JSON string`（E095 那条是 `'('`）。两条命令都在**给正则元字符加反斜杠**：`grep -v '^.*//.*v\(1[0-9]\)'` 与 `grep -n '生成\|勿手改\|BEGIN'`。
- **根因**：JSON 字符串里**只允许** `\" \\ \/ \b \f \n \r \t \uXXXX`（RFC 8259 §7）；模型把 shell 正则原样搬进来 ⇒ `\|` `\(` **非法** ⇒ `JsonException` ⇒ 整篇拒（**一坏全不跑**，fail-closed）。
- **为什么叫「同族第三个病因」（本条真正的教训）**：同一个症状（`[TOOL]` 块被拒）到今晚已有**三个不同**的病因 —— ① 键没引号 / 键名猜错（#74 / #113，协议 v18 + 带引号示例已覆盖）；② 串里有未转义换行（#127 / #130，`Diagnose` + 话术已覆盖）；③ **给正则元字符加反斜杠（本条）**。⇒ **话术只说一个病因时，换一个病因就等于没话术**（§十·51：已记的错因 ≠ 复发的错因）。
- **修法（本轮落地，零缓存代价）**：① `ToolArgs.Parse` 的 JSON 拒因提示从「只查换行」扩成**按形状报事实** —— 认出非法转义就**点名那一个字符** + 给可执行修法（把反斜杠翻倍 `\\|` / 直接写 `(` `|` / 整条命令用单引号包住 / `grep -F`）；② 拒绝话术（`ToolRunner.ToolFaceContract`）加一句「**串里只许有 JSON 的转义**」。**两处都不动协议区** ⇒ 不付冷启税。
- **闸门（正例 + 决定性负例，都真跑过）**：`ToolArgsJsonHintTests.解析_JSON串里的非法转义_必须指名那一条并给修法`（负例：`\|` `\(` 必须被拒、且话术里出现**点名**的那一句）+ `解析_照修法把反斜杠翻倍_必须放行`（正例：照修法翻倍后必须过、合法转义不许误伤、换行那条旧话术不许被挤掉）。**有牙自证**：把新分支临时拔成 `=> true` ⇒ 负例当场变红（1 失败 / 1 通过），恢复后 **2/2 绿**；全量 `dotnet test` **685/685**。
- **落点**：`gate`（= 上面两条闸门）；常驻半边已在 `KNOWLEDGE.md` §十·51。
- **同族**：坑 #130（「记进坑集」防不了复发 / 已记错因 ≠ 复发错因）· #113（形状示范会教坏模型）· #127（长内容塞 JSON）· §十·51 · §十·58（修法要枚举所有入口）。

## 158. **版本号是账本、正文字节是事实**：改了正文没升版本 ⇒ 三处互斥且**无闸门会喊**（2026-09-25 · [WB] · 作者令「看看协议区版本更新了没」当场量出）

- **现场（可复算）**：`svn cat -r955|957|958|959|963 src/AgentRuntime.Core/Protocol/ProtocolText.cs | grep 'public const string Version'` ⇒ **一律 `"21"`**；而 r958 的提交信息写「v22 协议区做净」、`ProtocolZoneTests` 注释写 v22、水位写「协议区 v22」。
- **事实**：正文**确实改了**（第 12 条补 `body:` 成品正文位 · 预算 1800→1900）—— 只有**账本没跟**。
- **为什么没被发现**：已有的副本闸门只盯「**文档 == 代码**」，**不盯「版本 == 正文」**；版本号只进账本、不进 prompt ⇒ 它错了没人看得见，而屏上/文档里**拿它当事实**。
- **修法**：① 版本 21→22 **做净五处**（代码常量 · 文档标题/版本史/规模/预算史 · 两条测试断言 · TUI 标签 · demo 样例重生）；② **新增闸门** `Gate_Protocol_VersionTracksTextBytes` —— 判据 = 正文 sha256 必须与**它自己声明的版本号**对得上（`KnownTextFingerprints` 从 v22 起逐版登记）。
- **落点**：`gate`（= 上面那条闸门）。
- **同族**：§十·11（水位线）· §十·48（屏面上的数必须来自事实产物）· #86/#119（自称只读却写账本）。

## 159. **归一化做了半程**：写入侧与比对侧只归一一边 ⇒ 集合**静默不命中**（2026-09-25 · [WB] · **生产 bug**，不是测试问题）

- **现场**：三条挂账红灯同族（09-22 起 `handoff/19` 登记）：`ToolReportTests`（测试侧期望值）· `SecurityGatewayTests`（污染集）· `PreAuthorizationTests`（预授权匹配）。
- **根因**：macOS 上 `/var` → `/private/var` 是符号链接；`ToolPaths.Normalize` **解真身**，而**比较侧仍用字面拼法** ⇒ 字符串相等**恒假**（§十·54 / 坑 #139）。
- **真实后果（远比「测试红」贵）**：① 污染集不命中 ⇒ 「本会话刚写出来的可执行文件」**静默不判可疑** ⇒ 洞 W2（写→执行链）；② 预授权不命中 ⇒ 人**签过字**的授权**静默降级**成「再问一次」。
- **修法**：新增 `ToolPaths.NormalizeOrSelf`（**安全版**：判不出真身 ⇒ **原样返回、不抛** —— 集合比对处抛异常会把「这个路径可疑」变成「整个判定炸掉」）；`GrantSet.NoteWritten / IsTainted` 与 `PreAuthorizationStore.Issue` 改用它；测试侧期望值改用**同一口径**。
- **落点**：`gate`（`SecurityGatewayTests.本会话写出来的可执行文件_分类器标为可疑` + `ToolReportTests.路径_不做围栏_任意绝对路径都算出真身`）。
- **同族**：坑 #139（判「是不是同一个目录」别比字符串）· §十·54。

## 160. **派生产物必须从正确配置构建**：重生成 **rc=0**，产物却还是旧的（2026-09-25 · [WB] 当场踩到）

- **现场**：`sh src/AgentRuntime.Tui/demo.sh` 跑完三段全绿，产物里却仍是 `v21` / 6534 字节 —— 因为该脚本跑的是 `bin/Debug/net10.0/AgentRuntime.Tui.dll`（**硬编码配置**），而我当时只编了 Release。
- **根因**：重生成类命令的**退出码只说「脚本跑完了」**，不说「产物是新的」（§十·47 同族）；跨配置的 dll 是**隐式前置**。
- **修法**：① 重生成前先按脚本**真实用到的配置**构建（本次 `-c Debug`）；② **判据 = 读产物里的版本/内容**（读 `v22` + 6791 字节 + 指纹 `5eb891b39e0d`），**不读退出码**；③ 用**独立校验器**（`sync-demo-doc.py --check`）随生成同场次复验。
- **落点**：`resident`（`KNOWLEDGE.md` §十·67）。
- **同族**：§十·47（退出码 0 ≠ 做成了）· §十·42（加了能力 ≠ 被用上）。
